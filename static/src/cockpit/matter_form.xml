<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <!-- The cockpit header: the contextual action(s) visible, everything
         marked o_ldm_more behind one "⋯" button. On a phone the first action
         stays visible and the rest share the menu. -->
    <t t-name="legal_department_management.StatusBarButtons">
        <div class="o_statusbar_buttons o_ldm_statusbar_buttons d-flex align-items-center align-content-around flex-wrap gap-1">
            <t t-if="env.isSmall">
                <t t-set="allSlots" t-value="mainSlotNames.concat(moreSlotNames)"/>
                <t t-set="firstSlot" t-value="mainSlotNames[0] || allSlots[0]"/>
                <t t-set="otherSlots" t-value="allSlots.filter((name) => name !== firstSlot)"/>
                <t t-if="firstSlot" t-slot="{{ firstSlot }}"/>
                <Dropdown t-if="otherSlots.length">
                    <button class="btn btn-secondary o_ldm_more_toggle" t-att-title="moreLabel" t-att-aria-label="moreLabel">
                        <i class="oi oi-fw oi-ellipsis-v" role="img" aria-hidden="true"/>
                    </button>
                    <t t-set-slot="content">
                        <t t-foreach="otherSlots" t-as="slot" t-key="slot">
                            <DropdownItem class="'o-dropdown-item-unstyled-button'">
                                <t t-slot="{{ slot }}"/>
                            </DropdownItem>
                        </t>
                    </t>
                </Dropdown>
            </t>
            <t t-else="">
                <t t-foreach="mainSlotNames" t-as="slot" t-key="slot">
                    <t t-slot="{{ slot }}"/>
                </t>
                <Dropdown t-if="moreSlotNames.length">
                    <button class="btn btn-secondary o_ldm_more_toggle" t-att-title="moreLabel" t-att-aria-label="moreLabel">
                        <i class="oi oi-fw oi-ellipsis-h" role="img" aria-hidden="true"/>
                    </button>
                    <t t-set-slot="content">
                        <t t-foreach="moreSlotNames" t-as="slot" t-key="slot">
                            <DropdownItem class="'o-dropdown-item-unstyled-button'">
                                <t t-slot="{{ slot }}"/>
                            </DropdownItem>
                        </t>
                    </t>
                </Dropdown>
            </t>
        </div>
    </t>

</templates>
