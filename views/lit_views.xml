<?xml version="1.0" encoding="utf-8"?>
<!-- Owned by the lit stream (docs/ldm/SPEC.md section 11): court sessions,
     court stages, judgments, statutory periods, deadlines and holidays. -->
<odoo>
    <!-- ================================================================
         Statutory periods
         ================================================================ -->
    <record id="view_ldm_appeal_rule_list" model="ir.ui.view">
        <field name="name">legal.appeal.rule.list</field>
        <field name="model">legal.appeal.rule</field>
        <field name="arch" type="xml">
            <list string="Statutory periods" expand="1" class="o_ldm_view">
                <field name="sequence" widget="handle"/>
                <field name="code"/>
                <field name="name"/>
                <field name="period_label"/>
                <field name="start_event"/>
                <field name="degree_ids" widget="many2many_tags" optional="show"/>
                <field name="verification" widget="ldm_pill"
                       decoration-success="verification == 'verified'"
                       decoration-warning="verification == 'secondary'"
                       decoration-danger="verification == 'unverified'"/>
                <field name="law" optional="hide"/>
                <field name="remedy" optional="hide"/>
                <field name="basis" optional="hide"/>
                <field name="peremptory" optional="hide"/>
                <field name="from_judgment" optional="hide"/>
                <field name="next_rule_id" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_appeal_rule_form" model="ir.ui.view">
        <field name="name">legal.appeal.rule.form</field>
        <field name="model">legal.appeal.rule</field>
        <field name="arch" type="xml">
            <form string="Statutory period" class="o_ldm_view">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_deadlines" type="object" class="oe_stat_button" icon="fa-hourglass-half">
                            <field name="deadline_count" widget="statinfo" string="Deadlines"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <field name="active" invisible="1"/>
                    <div class="oe_title">
                        <div class="text-muted"><field name="code" placeholder="Code, e.g. CIV-2" class="oe_inline"/></div>
                        <h1><field name="name" placeholder="e.g. Appeal of a first-instance judgment"/></h1>
                    </div>
                    <div class="alert alert-warning" role="status" invisible="verification == 'verified'">
                        This period is not yet checked against the law's own text. Confirm it before relying on it at a court.
                    </div>
                    <group>
                        <group string="The period" name="period">
                            <label for="days" string="Length"/>
                            <div class="o_row">
                                <field name="days"/>
                                <field name="unit"/>
                            </div>
                            <field name="start_event"/>
                            <field name="trigger_outcome" invisible="start_event not in ('hearing_outcome', 'event') and not trigger_outcome"/>
                            <field name="next_rule_id" options="{'no_create': True}"/>
                            <field name="max_months"/>
                            <field name="extends_on_holiday"/>
                            <field name="peremptory"/>
                        </group>
                        <group string="Where it applies" name="applies">
                            <field name="law"/>
                            <field name="remedy"/>
                            <field name="degree_ids" widget="many2many_tags" options="{'no_create': True}"/>
                            <field name="judgment_presence"/>
                            <field name="from_judgment"/>
                        </group>
                    </group>
                    <group string="Legal basis" name="basis">
                        <field name="basis"/>
                        <field name="source_url" widget="url"/>
                        <field name="verification" widget="radio" options="{'horizontal': true}"/>
                        <field name="note"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_ldm_appeal_rule_search" model="ir.ui.view">
        <field name="name">legal.appeal.rule.search</field>
        <field name="model">legal.appeal.rule</field>
        <field name="arch" type="xml">
            <search string="Statutory periods">
                <field name="name" filter_domain="['|', '|', ('name', 'ilike', self), ('code', 'ilike', self), ('basis', 'ilike', self)]"/>
                <field name="basis"/>
                <filter name="filter_from_judgment" string="Proposed from a judgment" domain="[('from_judgment', '=', True)]"/>
                <filter name="filter_not_verified" string="Not yet verified" domain="[('verification', '!=', 'verified')]"/>
                <separator/>
                <filter name="filter_archived" string="Archived" domain="[('active', '=', False)]"/>
                <group>
                    <filter name="group_law" string="Law" context="{'group_by': 'law'}"/>
                    <filter name="group_remedy" string="Remedy" context="{'group_by': 'remedy'}"/>
                    <filter name="group_verification" string="Confidence" context="{'group_by': 'verification'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_appeal_rule" model="ir.actions.act_window">
        <field name="name">Statutory periods</field>
        <field name="res_model">legal.appeal.rule</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ldm_appeal_rule_search"/>
        <field name="context">{'search_default_group_law': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No statutory periods</p>
            <p>Each period says how long there is to challenge a judgment or act on a decision, what starts it, its legal basis and how sure we are of it.</p>
        </field>
    </record>

    <!-- ================================================================
         Deadlines
         ================================================================ -->
    <record id="view_ldm_deadline_list" model="ir.ui.view">
        <field name="name">legal.deadline.list</field>
        <field name="model">legal.deadline</field>
        <field name="arch" type="xml">
            <list string="Deadlines" default_order="date_safe, id"
                  decoration-danger="state == 'missed'"
                  decoration-info="state == 'awaiting_service'"
                  decoration-muted="state in ('done', 'cancelled', 'lapsed')" class="o_ldm_view">
                <field name="name"/>
                <field name="task_id" optional="show"/>
                <field name="date_safe" string="Act by" widget="remaining_days" invisible="state not in ('open', 'missed')"/>
                <field name="date_deadline" string="Legal last day" optional="show"/>
                <field name="user_id" widget="many2one_avatar_user" optional="show"/>
                <field name="state" widget="ldm_pill"
                       decoration-info="state == 'awaiting_service'"
                       decoration-primary="state == 'open'"
                       decoration-success="state == 'done'"
                       decoration-danger="state == 'missed'"/>
                <field name="legal_company_id" optional="hide"/>
                <field name="rule_id" optional="hide"/>
                <field name="kind" optional="hide"/>
                <field name="rule_verification" optional="hide"/>
                <field name="our_action" optional="hide"/>
                <button name="action_ldm_mark_met" type="object" string="Met" icon="fa-check" title="Mark as met"
                        invisible="state not in ('open', 'awaiting_service') or not our_action"
                        groups="legal_department_management.group_ldm_clerk"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_deadline_form" model="ir.ui.view">
        <field name="name">legal.deadline.form</field>
        <field name="model">legal.deadline</field>
        <field name="arch" type="xml">
            <form string="Deadline" class="o_ldm_view">
                <header>
                    <button name="action_ldm_mark_met" type="object" string="Mark as met" class="btn-primary"
                            invisible="state not in ('open', 'awaiting_service') or not our_action"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_mark_met" type="object" string="Mark as used" class="btn-primary"
                            help="The other side or the authority acted within the period."
                            invisible="state not in ('open', 'awaiting_service') or our_action"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_cancel" type="object" string="Cancel"
                            invisible="state not in ('open', 'awaiting_service')"
                            groups="legal_department_management.group_legal_user"/>
                    <button name="action_ldm_reopen" type="object" string="Reopen"
                            invisible="state in ('open', 'awaiting_service')"
                            groups="legal_department_management.group_legal_manager"/>
                    <field name="state" widget="statusbar" statusbar_visible="open,done"/>
                </header>
                <sheet>
                    <field name="company_id" invisible="1"/>
                    <field name="source_model" invisible="1"/>
                    <field name="rolled" invisible="1"/>
                    <widget name="web_ribbon" title="Missed" bg_color="text-bg-danger" invisible="state != 'missed'"/>
                    <widget name="web_ribbon" title="Met" bg_color="text-bg-success" invisible="state != 'done'"/>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="e.g. Reply to the court's letter"/></h1>
                        <div class="d-flex flex-wrap gap-2">
                            <span class="badge text-bg-danger" invisible="not rule_peremptory">Cannot be extended</span>
                            <span class="badge text-bg-info" invisible="our_action">Runs for the other side</span>
                            <field name="rule_verification" widget="ldm_pill" invisible="not rule_id"
                                   decoration-success="rule_verification == 'verified'"
                                   decoration-warning="rule_verification == 'secondary'"
                                   decoration-danger="rule_verification == 'unverified'"/>
                        </div>
                    </div>
                    <div class="alert alert-info" role="status" invisible="state != 'awaiting_service'">
                        This period starts when the judgment or decision is served. Enter the notification date and the dates are counted.
                    </div>
                    <group>
                        <group string="When" name="dates">
                            <field name="date_start" readonly="source_model == 'legal.judgment'"/>
                            <field name="date_safe" string="Act by" readonly="rule_id"/>
                            <field name="date_deadline" string="Legal last day" readonly="rule_id"/>
                            <div colspan="2" class="text-muted small" invisible="not rolled">
                                The period ends on a weekend or public holiday, so the law gives until the next working day (Civil Procedure Law Art. 25(2)). Act by the earlier date.
                            </div>
                            <field name="remaining_days" invisible="state != 'open'"/>
                            <field name="date_done" invisible="not date_done"/>
                        </group>
                        <group string="Who and what" name="matter">
                            <field name="task_id" options="{'no_create': True}"/>
                            <field name="legal_company_id" invisible="task_id" options="{'no_create': True}"/>
                            <field name="judgment_id" invisible="not judgment_id" readonly="1"/>
                            <field name="user_id" widget="many2one_avatar_user"/>
                            <field name="kind"/>
                            <field name="our_action"/>
                        </group>
                    </group>
                    <group string="Statutory period" name="rule">
                        <field name="rule_id" options="{'no_create': True}"/>
                        <field name="rule_peremptory" invisible="1"/>
                        <field name="rule_basis" invisible="not rule_id"/>
                        <field name="rule_source_url" widget="url" invisible="not rule_source_url"/>
                        <field name="previous_deadline_id" invisible="not previous_deadline_id" readonly="1"/>
                        <field name="next_deadline_ids" widget="many2many_tags" invisible="not next_deadline_ids" readonly="1"/>
                        <field name="note"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_deadline_kanban" model="ir.ui.view">
        <field name="name">legal.deadline.kanban</field>
        <field name="model">legal.deadline</field>
        <field name="arch" type="xml">
            <kanban class="o_ldm_view o_ldm_kanban" default_order="date_safe, id" quick_create="false" sample="1">
                <field name="name"/>
                <field name="task_id"/>
                <field name="date_safe"/>
                <field name="state"/>
                <field name="our_action"/>
                <field name="user_id"/>
                <templates>
                    <t t-name="card">
                        <field name="name" class="fw-bold text-truncate"/>
                        <field name="task_id" class="text-muted small text-truncate"/>
                        <div class="small">
                            <span class="text-muted">Act by</span>
                            <field name="date_safe" widget="remaining_days" invisible="state != 'open'"/>
                            <field name="date_safe" invisible="state == 'open'"/>
                        </div>
                        <footer class="pt-1 align-items-center">
                            <field name="state" widget="ldm_pill" decoration-primary="state == 'open'"
                                   decoration-danger="state == 'missed'" decoration-success="state == 'done'"
                                   decoration-info="state == 'awaiting_service'"/>
                            <button name="action_ldm_mark_met" type="object" string="Met" class="btn btn-sm btn-secondary"
                                    invisible="state not in ('open', 'awaiting_service') or not our_action"
                                    groups="legal_department_management.group_ldm_clerk"/>
                            <field name="user_id" widget="many2one_avatar_user" class="ms-auto"/>
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_ldm_deadline_calendar" model="ir.ui.view">
        <field name="name">legal.deadline.calendar</field>
        <field name="model">legal.deadline</field>
        <field name="arch" type="xml">
            <calendar js_class="ldm_calendar" string="Deadlines" date_start="date_safe" color="user_id" mode="month" scales="month,week"
                      quick_create="0" show_unusual_days="1">
                <field name="task_id"/>
                <field name="date_deadline"/>
                <field name="state"/>
                <field name="user_id" avatar_field="avatar_128" filters="1"/>
            </calendar>
        </field>
    </record>

    <record id="view_ldm_deadline_search" model="ir.ui.view">
        <field name="name">legal.deadline.search</field>
        <field name="model">legal.deadline</field>
        <field name="arch" type="xml">
            <search string="Deadlines">
                <field name="name"/>
                <field name="task_id"/>
                <field name="legal_company_id"/>
                <field name="rule_id"/>
                <field name="user_id"/>
                <filter name="filter_mine" string="Mine" domain="[('user_id', '=', uid)]"/>
                <separator/>
                <filter name="filter_open" string="Open" domain="[('state', 'in', ('open', 'awaiting_service'))]"/>
                <filter name="filter_awaiting" string="Awaiting notification" domain="[('state', '=', 'awaiting_service')]"/>
                <filter name="filter_missed" string="Missed" domain="[('state', '=', 'missed')]"/>
                <separator/>
                <filter name="filter_week" string="Ends within 7 days"
                        domain="[('state', '=', 'open'), ('date_safe', '&lt;=', (context_today() + relativedelta(days=7)).strftime('%Y-%m-%d'))]"/>
                <filter name="filter_challenges" string="Challenge windows" domain="[('kind', '=', 'appeal')]"/>
                <filter name="filter_ours" string="Ours to act on" domain="[('our_action', '=', True)]"/>
                <separator/>
                <filter name="filter_date" string="Act by" date="date_safe"/>
                <group>
                    <filter name="group_state" string="Status" context="{'group_by': 'state'}"/>
                    <filter name="group_kind" string="Kind" context="{'group_by': 'kind'}"/>
                    <filter name="group_user" string="Responsible" context="{'group_by': 'user_id'}"/>
                    <filter name="group_task" string="Matter" context="{'group_by': 'task_id'}"/>
                    <filter name="group_week" string="Week" context="{'group_by': 'date_safe:week'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_deadline" model="ir.actions.act_window">
        <field name="name">Deadlines</field>
        <field name="res_model">legal.deadline</field>
        <field name="view_mode">list,kanban,calendar,form</field>
        <field name="search_view_id" ref="view_ldm_deadline_search"/>
        <field name="context">{'search_default_filter_open': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No open deadlines</p>
            <p>Record a judgment on a lawsuit and its challenge periods appear here, counted from the notification date.</p>
        </field>
    </record>

    <!-- ================================================================
         Judgments
         ================================================================ -->
    <record id="view_ldm_judgment_form" model="ir.ui.view">
        <field name="name">legal.judgment.form</field>
        <field name="model">legal.judgment</field>
        <field name="arch" type="xml">
            <form string="Judgment" class="o_ldm_view">
                <header>
                    <button name="action_ldm_lodge_challenge" type="object" string="Lodge a challenge" class="btn-primary"
                            invisible="not ldm_can_challenge or not id"
                            groups="legal_department_management.group_legal_user"/>
                </header>
                <sheet>
                    <field name="currency_id" invisible="1"/>
                    <field name="company_id" invisible="1"/>
                    <field name="ldm_can_challenge" invisible="1"/>
                    <group>
                        <group string="The judgment" name="judgment">
                            <field name="task_id" invisible="context.get('default_task_id')" options="{'no_create': True}"/>
                            <field name="date"/>
                            <field name="result" widget="selection_badge"/>
                            <field name="amount_awarded" options="{'hide_trailing_zeros': True}"/>
                            <field name="notified_date"/>
                            <field name="final_date" invisible="not final_date and not id"/>
                        </group>
                        <group string="The court" name="court">
                            <field name="department_id" domain="[('body_kind', '=', 'court')]" options="{'no_create': True}"/>
                            <field name="court_degree"/>
                            <field name="court_stage"/>
                            <field name="law"/>
                            <field name="pronounced_in_presence"/>
                            <field name="in_absentia"/>
                        </group>
                    </group>
                    <div class="alert alert-info" role="status" invisible="notified_date or not deadline_ids">
                        Most challenge periods count from the day after the judgment is served. Enter the notification date to count them.
                    </div>
                    <field name="summary" placeholder="What the court decided, in a few lines."/>
                    <separator string="Deadlines" invisible="not deadline_ids"/>
                    <field name="deadline_ids" readonly="1" invisible="not deadline_ids">
                        <list create="0" delete="0"
                              decoration-danger="state == 'missed'"
                              decoration-info="state == 'awaiting_service'"
                              decoration-muted="state in ('done', 'cancelled', 'lapsed')">
                            <field name="name"/>
                            <field name="date_safe" string="Act by"/>
                            <field name="date_deadline" string="Legal last day"/>
                            <field name="rule_verification" widget="ldm_pill" optional="show"
                                   decoration-success="rule_verification == 'verified'"
                                   decoration-warning="rule_verification == 'secondary'"
                                   decoration-danger="rule_verification == 'unverified'"/>
                            <field name="state" widget="ldm_pill"/>
                        </list>
                    </field>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_judgment_list" model="ir.ui.view">
        <field name="name">legal.judgment.list</field>
        <field name="model">legal.judgment</field>
        <field name="arch" type="xml">
            <list string="Judgments" class="o_ldm_view">
                <field name="date"/>
                <field name="task_id"/>
                <field name="department_id"/>
                <field name="result"/>
                <field name="notified_date"/>
                <field name="next_deadline_label"/>
                <field name="final_date" optional="hide"/>
                <field name="amount_awarded" optional="hide" options="{'hide_trailing_zeros': True}"/>
                <field name="currency_id" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- ================================================================
         Court sessions
         ================================================================ -->
    <record id="view_ldm_hearing_list" model="ir.ui.view">
        <field name="name">legal.hearing.list</field>
        <field name="model">legal.hearing</field>
        <field name="arch" type="xml">
            <list string="Court sessions" default_order="date, time, id" expand="1"
                  decoration-muted="state != 'planned'" class="o_ldm_view">
                <field name="date"/>
                <field name="time" widget="ldm_time" optional="show"/>
                <field name="task_id"/>
                <field name="department_id"/>
                <field name="attending_user_id" widget="many2one_avatar_user"/>
                <field name="state" widget="ldm_pill" decoration-primary="state == 'planned'" decoration-success="state == 'held'" optional="hide"/>
                <field name="outcome" optional="show"/>
                <field name="court_case_number" optional="hide"/>
                <field name="legal_company_id" optional="hide"/>
                <field name="purpose" optional="hide"/>
                <field name="substitute_partner_id" optional="hide"/>
                <field name="needs_substitution_letter" optional="hide"/>
                <button name="action_ldm_record_outcome" type="object" string="Record" icon="fa-gavel"
                        title="Record what happened" invisible="state != 'planned'"
                        groups="legal_department_management.group_ldm_clerk"/>
            </list>
        </field>
    </record>

    <!-- On a phone the web client opens the kanban instead of the list. -->
    <record id="view_ldm_hearing_kanban" model="ir.ui.view">
        <field name="name">legal.hearing.kanban</field>
        <field name="model">legal.hearing</field>
        <field name="arch" type="xml">
            <kanban class="o_ldm_view o_ldm_kanban" default_order="date, time, id" quick_create="false" sample="1">
                <field name="date"/>
                <field name="time"/>
                <field name="task_id"/>
                <field name="department_id"/>
                <field name="attending_user_id"/>
                <field name="state"/>
                <templates>
                    <t t-name="card">
                        <div class="d-flex gap-2 fw-bold">
                            <field name="date"/>
                            <field name="time" widget="ldm_time" invisible="not time"/>
                        </div>
                        <field name="task_id" class="text-truncate"/>
                        <field name="department_id" class="text-muted small text-truncate"/>
                        <footer class="pt-1 align-items-center">
                            <field name="state" widget="ldm_pill" decoration-primary="state == 'planned'" decoration-success="state == 'held'"/>
                            <button name="action_ldm_record_outcome" type="object" string="Record" class="btn btn-sm btn-primary"
                                    invisible="state != 'planned'" groups="legal_department_management.group_ldm_clerk"/>
                            <field name="attending_user_id" widget="many2one_avatar_user" class="ms-auto"/>
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_ldm_hearing_form" model="ir.ui.view">
        <field name="name">legal.hearing.form</field>
        <field name="model">legal.hearing</field>
        <field name="arch" type="xml">
            <form string="Court session" class="o_ldm_view">
                <header>
                    <button name="action_ldm_record_outcome" type="object" string="Record what happened" class="btn-primary"
                            invisible="state != 'planned' or not id"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_print_substitution_letter" type="object" string="Substitution letter"
                            invisible="not needs_substitution_letter or not id"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <field name="state" widget="statusbar" statusbar_visible="planned,held"/>
                </header>
                <sheet>
                    <field name="needs_substitution_letter" invisible="1"/>
                    <div class="alert alert-danger" role="alert" invisible="not licence_warning">
                        <field name="licence_warning" readonly="1"/>
                    </div>
                    <div class="alert alert-warning" role="status" invisible="not substitution_warning">
                        <field name="substitution_warning" readonly="1"/>
                    </div>
                    <div class="alert alert-warning" role="status" invisible="not date_warning">
                        <field name="date_warning" readonly="1"/>
                    </div>
                    <div class="oe_title">
                        <div class="text-muted"><field name="court_case_number" readonly="1"/></div>
                        <h1><field name="task_id" class="w-100" options="{'no_create': True}" placeholder="Matter"/></h1>
                    </div>
                    <group>
                        <group name="when">
                            <field name="date"/>
                            <field name="time" widget="ldm_time" placeholder="Not set yet"/>
                            <field name="department_id" domain="[('body_kind', '=', 'court')]" options="{'no_create': True}"/>
                            <field name="court_room"/>
                            <field name="purpose"/>
                        </group>
                        <group name="who">
                            <field name="attending_user_id" widget="many2one_avatar_user"/>
                            <field name="substitute_partner_id"/>
                            <field name="lawyer_id" widget="many2one_avatar_user" string="Responsible for the matter"/>
                            <field name="kind"/>
                        </group>
                    </group>
                    <group string="What happened" name="outcome" invisible="state == 'planned'">
                        <group>
                            <field name="outcome"/>
                            <field name="attendance"/>
                            <field name="next_hearing_id" options="{'no_create': True}"/>
                            <field name="previous_hearing_id" invisible="not previous_hearing_id"/>
                        </group>
                        <group>
                            <field name="needed_before"/>
                            <field name="minutes_attachment_id" options="{'no_create': True}"/>
                        </group>
                        <field name="outcome_note" colspan="2" placeholder="What the court said and did"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_hearing_calendar" model="ir.ui.view">
        <field name="name">legal.hearing.calendar</field>
        <field name="model">legal.hearing</field>
        <field name="arch" type="xml">
            <calendar js_class="ldm_calendar" string="Court sessions" date_start="date" color="attending_user_id" mode="month"
                      scales="month,week" quick_create="0" show_unusual_days="1">
                <field name="task_id"/>
                <field name="time" widget="ldm_time" invisible="not time"/>
                <field name="department_id"/>
                <field name="court_case_number"/>
                <field name="state"/>
                <field name="attending_user_id" avatar_field="avatar_128" filters="1"/>
            </calendar>
        </field>
    </record>

    <record id="view_ldm_hearing_search" model="ir.ui.view">
        <field name="name">legal.hearing.search</field>
        <field name="model">legal.hearing</field>
        <field name="arch" type="xml">
            <search string="Court sessions">
                <field name="task_id"/>
                <field name="legal_company_id"/>
                <field name="department_id"/>
                <field name="attending_user_id"/>
                <filter name="filter_mine" string="Mine" domain="['|', ('attending_user_id', '=', uid), ('lawyer_id', '=', uid)]"/>
                <separator/>
                <filter name="filter_upcoming" string="Upcoming"
                        domain="[('state', '=', 'planned'), ('date', '&gt;=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter name="filter_next_14" string="Next 14 days"
                        domain="[('state', '=', 'planned'), ('date', '&gt;=', context_today().strftime('%Y-%m-%d')), ('date', '&lt;=', (context_today() + relativedelta(days=14)).strftime('%Y-%m-%d'))]"/>
                <filter name="filter_unrecorded" string="Held, not yet recorded"
                        domain="[('state', '=', 'planned'), ('date', '&lt;', context_today().strftime('%Y-%m-%d'))]"/>
                <filter name="filter_held" string="Recorded" domain="[('state', '=', 'held')]"/>
                <filter name="filter_no_lawyer" string="Nobody attending"
                        domain="[('state', '=', 'planned'), ('attending_user_id', '=', False), ('substitute_partner_id', '=', False)]"/>
                <separator/>
                <filter name="filter_date" string="Date" date="date"/>
                <group>
                    <filter name="group_court" string="Court" context="{'group_by': 'department_id'}"/>
                    <filter name="group_lawyer" string="Attending" context="{'group_by': 'attending_user_id'}"/>
                    <filter name="group_day" string="Day" context="{'group_by': 'date:day'}"/>
                    <filter name="group_state" string="Status" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- The native session calendar: the agenda's "Month" opens it. -->
    <record id="action_ldm_hearing_calendar" model="ir.actions.act_window">
        <field name="name">Court sessions</field>
        <field name="res_model">legal.hearing</field>
        <field name="path">court-sessions</field>
        <field name="view_mode">calendar,list,kanban,form</field>
        <field name="search_view_id" ref="view_ldm_hearing_search"/>
        <field name="context">{'ldm_hearing_title': 'calendar'}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No court sessions</p>
            <p>Sessions are added on a lawsuit's Sessions tab, or fixed by recording what happened at the previous one.</p>
        </field>
    </record>

    <!-- Until the workspace's agenda replaces it: the next 14 days, by day. -->
    <record id="action_ldm_agenda" model="ir.actions.act_window">
        <field name="name">Agenda</field>
        <field name="res_model">legal.hearing</field>
        <field name="view_mode">list,kanban,calendar,form</field>
        <field name="search_view_id" ref="view_ldm_hearing_search"/>
        <field name="domain">[]</field>
        <field name="context">{'search_default_filter_next_14': 1, 'search_default_group_day': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No sessions in the next 14 days</p>
            <p>Sessions are added on a lawsuit's Sessions tab, or fixed by recording what happened at the previous one.</p>
        </field>
    </record>

    <!-- ================================================================
         Public holidays
         ================================================================ -->
    <record id="view_ldm_holiday_list" model="ir.ui.view">
        <field name="name">legal.holiday.list</field>
        <field name="model">legal.holiday</field>
        <field name="arch" type="xml">
            <list string="Public holidays" editable="bottom" default_order="date_from" expand="1" class="o_ldm_view">
                <field name="name"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="weekday_from" optional="show"/>
                <field name="day_count" optional="show"/>
                <field name="estimated" widget="boolean_toggle"/>
                <field name="governorate" optional="hide"/>
                <field name="calendar_id" optional="hide" options="{'no_create': True}"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_holiday_form" model="ir.ui.view">
        <field name="name">legal.holiday.form</field>
        <field name="model">legal.holiday</field>
        <field name="arch" type="xml">
            <form string="Public holiday" class="o_ldm_view">
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="e.g. Eid al-Adha"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="date_from"/>
                            <field name="date_to"/>
                            <field name="estimated"/>
                        </group>
                        <group>
                            <field name="governorate"/>
                            <field name="calendar_id" options="{'no_create': True}"/>
                        </group>
                    </group>
                    <p class="text-muted">
                        A deadline whose last day falls on this holiday moves to the next working day, and the lawyers whose dates move are told on their matters.
                    </p>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_holiday_search" model="ir.ui.view">
        <field name="name">legal.holiday.search</field>
        <field name="model">legal.holiday</field>
        <field name="arch" type="xml">
            <search string="Public holidays">
                <field name="name"/>
                <filter name="filter_estimated" string="Estimated dates" domain="[('estimated', '=', True)]"/>
                <filter name="filter_coming" string="From today"
                        domain="[('date_to', '&gt;=', context_today().strftime('%Y-%m-%d'))]"/>
                <group>
                    <filter name="group_year" string="Year" context="{'group_by': 'year'}"/>
                    <filter name="group_governorate" string="Governorate" context="{'group_by': 'governorate'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_holiday" model="ir.actions.act_window">
        <field name="name">Public holidays</field>
        <field name="res_model">legal.holiday</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ldm_holiday_search"/>
        <field name="context">{'search_default_group_year': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No holidays entered</p>
            <p>Enter the Eid dates when the endowment offices announce them, and any day the Council of Ministers adds. Deadlines that end on a holiday move to the next working day.</p>
        </field>
    </record>

    <!-- ================================================================
         Dialogs
         ================================================================ -->
    <record id="view_ldm_hearing_outcome_wizard_form" model="ir.ui.view">
        <field name="name">legal.hearing.outcome.wizard.form</field>
        <field name="model">legal.hearing.outcome.wizard</field>
        <field name="arch" type="xml">
            <form string="Record the session" class="o_ldm_view o_ldm_outcome_dialog">
                <field name="hearing_id" invisible="1"/>
                <field name="outcome" invisible="1"/>
                <field name="show_next" invisible="1"/>
                <field name="next_required" invisible="1"/>
                <field name="currency_id" invisible="1"/>
                <div class="text-muted mb-3"><field name="session_label" readonly="1"/></div>
                <group name="what">
                    <field name="main_outcome" widget="selection_badge"/>
                    <field name="more_outcome" invisible="main_outcome != 'more'"/>
                    <field name="attendance" widget="selection_badge" options="{'size': 'sm'}"/>
                </group>
                <group name="next" invisible="not show_next">
                    <label for="next_date"/>
                    <div class="o_row">
                        <field name="next_date" default_focus="1"/>
                        <field name="next_time" widget="ldm_time" placeholder="Time"/>
                    </div>
                    <div colspan="2" class="text-warning" invisible="not next_date_warning">
                        <field name="next_date_warning" readonly="1"/>
                    </div>
                    <label for="needed_before"/>
                    <div class="o_row">
                        <field name="needed_before" placeholder="e.g. Bring the expert's report"/>
                    </div>
                    <label for="needed_days" invisible="not needed_before"/>
                    <field name="needed_days" nolabel="1" invisible="not needed_before"/>
                </group>
                <group name="judgment" string="Judgment" invisible="outcome != 'judgment'">
                    <group>
                        <field name="judgment_date"/>
                        <field name="result" widget="selection_badge" options="{'size': 'sm'}"/>
                        <field name="amount_awarded" options="{'hide_trailing_zeros': True}"/>
                        <field name="notified_date"/>
                    </group>
                    <group>
                        <field name="law"/>
                        <field name="court_degree"/>
                        <field name="pronounced_in_presence"/>
                        <field name="in_absentia"/>
                    </group>
                </group>
                <div name="preview" class="mb-3" invisible="outcome != 'judgment'">
                    <div class="fw-bold mb-1">Deadlines this judgment opens</div>
                    <field name="deadline_preview" readonly="1"/>
                </div>
                <group name="period" invisible="not rule_id or outcome == 'judgment'">
                    <field name="rule_id" readonly="1" options="{'no_open': True}"/>
                    <field name="add_period"/>
                    <field name="period_start" invisible="not add_period"/>
                    <field name="period_preview" invisible="not add_period"/>
                </group>
                <group name="note">
                    <field name="note" placeholder="Anything worth remembering from the session"/>
                </group>
                <footer>
                    <button name="action_confirm" type="object" string="Save" class="btn-primary" data-hotkey="q"/>
                    <button string="Discard" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_judgment_challenge_wizard_form" model="ir.ui.view">
        <field name="name">legal.judgment.challenge.wizard.form</field>
        <field name="model">legal.judgment.challenge.wizard</field>
        <field name="arch" type="xml">
            <form string="Lodge a challenge" class="o_ldm_view">
                <field name="judgment_id" invisible="1"/>
                <group>
                    <field name="deadline_id" options="{'no_create': True, 'no_open': True}"/>
                    <field name="date_lodged"/>
                    <field name="stage"/>
                    <field name="department_id" options="{'no_create': True}"/>
                    <label for="case_number"/>
                    <div class="o_row">
                        <field name="case_number" placeholder="e.g. 1234/b"/>
                        <field name="case_year" options="{'enable_formatting': False}"/>
                    </div>
                </group>
                <p class="text-muted">
                    The challenge's period is marked as met, the other open challenge periods of this judgment close, and the next court stage opens on the matter.
                </p>
                <footer>
                    <button name="action_confirm" type="object" string="Lodge" class="btn-primary" data-hotkey="q"/>
                    <button string="Discard" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_hearing_roll_wizard_form" model="ir.ui.view">
        <field name="name">legal.hearing.roll.wizard.form</field>
        <field name="model">legal.hearing.roll.wizard</field>
        <field name="arch" type="xml">
            <form string="Hearing roll" class="o_ldm_view">
                <group>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                        <field name="include_held"/>
                    </group>
                    <group>
                        <field name="user_ids" widget="many2many_tags_avatar" options="{'no_create': True}"/>
                        <field name="department_ids" widget="many2many_tags" options="{'no_create': True}"/>
                    </group>
                </group>
                <footer>
                    <button name="action_print" type="object" string="Print" class="btn-primary" data-hotkey="q"/>
                    <button string="Discard" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_ldm_hearing_roll_wizard" model="ir.actions.act_window">
        <field name="name">Hearing roll</field>
        <field name="res_model">legal.hearing.roll.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_ldm_hearing_roll" name="Hearing roll" parent="menu_ldm_reporting" sequence="2"
              action="action_ldm_hearing_roll_wizard" groups="legal_department_management.group_ldm_clerk"/>

    <!-- ================================================================
         The lawsuit cockpit: sessions, court stages, judgments, deadlines
         ================================================================ -->
    <record id="view_legal_task_form_lit" model="ir.ui.view">
        <field name="name">legal.task.form.litigation</field>
        <field name="model">legal.task</field>
        <field name="inherit_id" ref="view_legal_task_form"/>
        <field name="arch" type="xml">
            <!-- A session whose day has come and whose outcome is not written up. -->
            <xpath expr="//header/button[@name='action_set_done']" position="before">
                <field name="ldm_session_to_record" invisible="1"/>
                <button name="action_ldm_record_outcome" type="object" string="Record the session"
                        invisible="not ldm_session_to_record or state in ('done', 'cancelled')"
                        groups="legal_department_management.group_ldm_clerk"/>
            </xpath>
            <xpath expr="//field[@name='court_case_number']" position="attributes">
                <attribute name="readonly">court_stage_ids</attribute>
            </xpath>

            <!-- Sessions -->
            <xpath expr="//field[@name='hearing_ids']" position="attributes">
                <attribute name="context">{'default_task_id': id, 'default_department_id': department_id, 'default_attending_user_id': lawyer_id}</attribute>
            </xpath>
            <xpath expr="//field[@name='hearing_ids']/list" position="attributes">
                <attribute name="decoration-muted">state != 'planned'</attribute>
                <attribute name="default_order">date, time, id</attribute>
            </xpath>
            <xpath expr="//field[@name='hearing_ids']/list/field[@name='date']" position="after">
                <field name="time" widget="ldm_time" optional="show"/>
            </xpath>
            <xpath expr="//field[@name='hearing_ids']/list/field[@name='department_id']" position="attributes">
                <attribute name="domain">[('body_kind', '=', 'court')]</attribute>
                <attribute name="options">{'no_create': True}</attribute>
            </xpath>
            <xpath expr="//field[@name='hearing_ids']/list/field[@name='state']" position="attributes">
                <attribute name="readonly">1</attribute>
                <attribute name="decoration-info">state == 'planned'</attribute>
                <attribute name="decoration-success">state == 'held'</attribute>
            </xpath>
            <xpath expr="//field[@name='hearing_ids']/list/field[@name='outcome']" position="attributes">
                <attribute name="readonly">1</attribute>
            </xpath>
            <xpath expr="//field[@name='hearing_ids']/list" position="inside">
                <field name="substitute_partner_id" optional="hide"/>
                <button name="action_ldm_record_outcome" type="object" string="Record" icon="fa-gavel"
                        title="Record what happened" invisible="state != 'planned' or not id"
                        groups="legal_department_management.group_ldm_clerk"/>
            </xpath>

            <!-- Case: court stages, then judgments, deadlines and parties -->
            <xpath expr="//field[@name='court_stage']" position="attributes">
                <attribute name="readonly">court_stage_ids</attribute>
            </xpath>
            <xpath expr="//page[@name='case']//field[@name='matter_value']" position="attributes">
                <attribute name="options">{'hide_trailing_zeros': True}</attribute>
            </xpath>
            <xpath expr="//field[@name='court_stage_ids']" position="before">
                <separator string="Court stages"/>
            </xpath>
            <xpath expr="//field[@name='court_stage_ids']" position="attributes">
                <attribute name="context">{'default_task_id': id, 'default_department_id': department_id}</attribute>
            </xpath>
            <xpath expr="//field[@name='court_stage_ids']/list/field[@name='department_id']" position="attributes">
                <attribute name="domain">[('body_kind', 'in', ('court', 'execution'))]</attribute>
                <attribute name="options">{'no_create': True}</attribute>
            </xpath>
            <xpath expr="//field[@name='court_stage_ids']/list/field[@name='case_number']" position="after">
                <field name="case_year" options="{'enable_formatting': False}" optional="show"/>
            </xpath>
            <xpath expr="//field[@name='court_stage_ids']/list" position="inside">
                <field name="execution_file_number" optional="hide"/>
                <field name="notification_date" optional="hide"/>
                <field name="judgment_id" optional="hide" readonly="1"/>
            </xpath>
            <!-- Cards for the phone, where the web client prefers a kanban. -->
            <xpath expr="//field[@name='court_stage_ids']" position="inside">
                <kanban>
                    <field name="stage"/>
                    <field name="department_id"/>
                    <field name="case_number"/>
                    <field name="case_year"/>
                    <field name="date_filed"/>
                    <templates>
                        <t t-name="card">
                            <div class="d-flex gap-2">
                                <field name="stage" class="fw-bold"/>
                                <field name="date_filed" class="ms-auto text-muted"/>
                            </div>
                            <field name="department_id" class="text-truncate"/>
                            <div class="text-muted small">
                                <field name="case_number"/>
                                <span t-if="record.case_year.raw_value"> · <t t-esc="record.case_year.raw_value"/></span>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </xpath>
            <xpath expr="//field[@name='judgment_ids']" position="inside">
                <kanban>
                    <field name="date"/>
                    <field name="result"/>
                    <field name="department_id"/>
                    <field name="next_deadline_label"/>
                    <field name="ldm_can_challenge"/>
                    <templates>
                        <t t-name="card">
                            <div class="d-flex gap-2">
                                <field name="date" class="fw-bold"/>
                                <field name="result" class="ms-auto"/>
                            </div>
                            <field name="department_id" class="text-truncate"/>
                            <field name="next_deadline_label" class="small"/>
                            <footer t-if="record.ldm_can_challenge.raw_value">
                                <button name="action_ldm_lodge_challenge" type="object" string="Lodge a challenge"
                                        class="btn btn-sm btn-secondary"
                                        groups="legal_department_management.group_legal_user"/>
                            </footer>
                        </t>
                    </templates>
                </kanban>
            </xpath>
            <xpath expr="//field[@name='deadline_ids']" position="inside">
                <kanban>
                    <field name="name"/>
                    <field name="date_safe"/>
                    <field name="date_deadline"/>
                    <field name="state"/>
                    <field name="our_action"/>
                    <templates>
                        <t t-name="card">
                            <field name="name" class="fw-bold text-truncate"/>
                            <div class="small">
                                <span class="text-muted">Act by</span>
                                <field name="date_safe"/>
                            </div>
                            <footer class="align-items-center">
                                <field name="state" widget="ldm_pill" decoration-primary="state == 'open'"
                                       decoration-danger="state == 'missed'" decoration-success="state == 'done'"
                                       decoration-info="state == 'awaiting_service'"/>
                                <button name="action_ldm_mark_met" type="object" string="Met" class="btn btn-sm btn-secondary ms-auto"
                                        invisible="state not in ('open', 'awaiting_service') or not our_action"
                                        groups="legal_department_management.group_ldm_clerk"/>
                            </footer>
                        </t>
                    </templates>
                </kanban>
            </xpath>
            <xpath expr="//field[@name='court_stage_ids']" position="after">
                <separator string="Judgments"/>
                <xpath expr="//field[@name='judgment_ids']" position="move"/>
                <separator string="Deadlines"/>
                <xpath expr="//field[@name='deadline_ids']" position="move"/>
                <separator string="Parties"/>
                <xpath expr="//field[@name='party_ids']" position="move"/>
            </xpath>
            <xpath expr="//field[@name='judgment_ids']/list" position="inside">
                <field name="next_deadline_label" string="Next deadline"/>
                <field name="final_date" optional="hide"/>
                <field name="ldm_can_challenge" column_invisible="1"/>
                <button name="action_ldm_lodge_challenge" type="object" string="Lodge a challenge" icon="fa-level-up"
                        title="Lodge a challenge against this judgment" invisible="not ldm_can_challenge"
                        groups="legal_department_management.group_legal_user"/>
            </xpath>
            <xpath expr="//field[@name='deadline_ids']/list" position="attributes">
                <attribute name="decoration-danger">state == 'missed' or (state == 'open' and remaining_days &lt; 3)</attribute>
                <attribute name="decoration-info">state == 'awaiting_service'</attribute>
                <attribute name="decoration-muted">state in ('done', 'cancelled', 'lapsed')</attribute>
            </xpath>
            <xpath expr="//field[@name='deadline_ids']/list/field[@name='date_safe']" position="attributes">
                <attribute name="string">Act by</attribute>
                <attribute name="readonly">rule_id</attribute>
            </xpath>
            <xpath expr="//field[@name='deadline_ids']/list/field[@name='date_deadline']" position="attributes">
                <attribute name="string">Legal last day</attribute>
                <attribute name="optional">show</attribute>
                <attribute name="readonly">rule_id</attribute>
            </xpath>
            <xpath expr="//field[@name='deadline_ids']/list/field[@name='remaining_days']" position="attributes">
                <attribute name="optional">hide</attribute>
            </xpath>
            <xpath expr="//field[@name='deadline_ids']/list/field[@name='state']" position="attributes">
                <attribute name="readonly">1</attribute>
                <attribute name="decoration-info">state == 'awaiting_service'</attribute>
                <attribute name="decoration-warning">state == 'open'</attribute>
                <attribute name="decoration-success">state == 'done'</attribute>
                <attribute name="decoration-danger">state == 'missed'</attribute>
            </xpath>
            <xpath expr="//field[@name='deadline_ids']/list" position="inside">
                <field name="rule_id" optional="hide" options="{'no_create': True}"/>
                <field name="our_action" column_invisible="1"/>
                <button name="action_ldm_mark_met" type="object" string="Met" icon="fa-check" title="Mark as met"
                        invisible="state not in ('open', 'awaiting_service') or not our_action or not id"
                        groups="legal_department_management.group_ldm_clerk"/>
            </xpath>
        </field>
    </record>
</odoo>
