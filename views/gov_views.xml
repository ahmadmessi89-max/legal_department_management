<?xml version="1.0" encoding="utf-8"?>
<!-- Owned by the gov stream (docs/ldm/SPEC.md section 11). Loaded after the
     menus, so it can add under existing folders and redefine the foundation's
     placeholder actions with real views. -->
<odoo>
    <!-- =====================================================================
         Directory of bodies and courts: read-only, for everyone in the team
         ===================================================================== -->
    <record id="view_ldm_body_directory_kanban" model="ir.ui.view">
        <field name="name">legal.department.directory.kanban</field>
        <field name="model">legal.department</field>
        <field name="priority">32</field>
        <field name="arch" type="xml">
            <kanban create="0" edit="0" delete="0" class="o_ldm_view o_ldm_kanban" sample="1">
                <field name="name"/>
                <field name="ministry_id"/>
                <field name="body_kind"/>
                <field name="working_hours"/>
                <field name="phone"/>
                <templates>
                    <t t-name="card">
                        <field name="name" class="fw-bold text-truncate"/>
                        <div class="text-muted small text-truncate">
                            <field name="ministry_id"/>
                        </div>
                        <footer class="pt-1 small">
                            <span t-if="record.working_hours.raw_value" class="text-truncate">
                                <i class="fa fa-clock-o" title="Opening hours" role="img"/>
                                <field name="working_hours" class="ms-1"/>
                            </span>
                            <span t-if="record.phone.raw_value" class="ms-auto">
                                <i class="fa fa-phone" title="Phone" role="img"/>
                                <field name="phone" class="ms-1"/>
                            </span>
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_ldm_body_directory_list" model="ir.ui.view">
        <field name="name">legal.department.directory.list</field>
        <field name="model">legal.department</field>
        <field name="priority">32</field>
        <field name="arch" type="xml">
            <list string="Bodies and courts" create="0" edit="0" delete="0" class="o_ldm_view">
                <field name="name"/>
                <field name="ministry_id"/>
                <field name="body_kind" optional="show"/>
                <field name="working_hours" optional="show"/>
                <field name="phone" optional="show"/>
                <field name="location_url" widget="url" text="Map" optional="show" invisible="not location_url"/>
                <field name="governorate" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_body_directory_form" model="ir.ui.view">
        <field name="name">legal.department.directory.form</field>
        <field name="model">legal.department</field>
        <field name="priority">32</field>
        <field name="arch" type="xml">
            <form string="Body or court" create="0" edit="0" delete="0" class="o_ldm_view">
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                        <field name="ministry_id" readonly="1"/>
                    </div>
                    <group>
                        <group name="identity">
                            <field name="body_kind" readonly="1"/>
                            <field name="court_degree" readonly="1" invisible="not court_degree"/>
                            <field name="parent_id" readonly="1" invisible="not parent_id"/>
                            <field name="governorate" readonly="1" invisible="not governorate"/>
                        </group>
                        <group name="practical">
                            <field name="working_hours" readonly="1"/>
                            <field name="phone" widget="phone" readonly="1" options="{'enable_sms': False}"/>
                            <field name="location_url" widget="url" text="Open the map" readonly="1" invisible="not location_url"/>
                            <field name="target_days" readonly="1" invisible="not target_days"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="contacts" string="Contacts">
                            <field name="contact_ids" readonly="1">
                                <kanban>
                                    <field name="name"/>
                                    <field name="role"/>
                                    <field name="phone"/>
                                    <templates>
                                        <t t-name="card">
                                            <field name="name" class="fw-bold"/>
                                            <field name="role" class="text-muted small"/>
                                            <field name="phone" widget="phone" options="{'enable_sms': False}"/>
                                        </t>
                                    </templates>
                                </kanban>
                                <list>
                                    <field name="name"/>
                                    <field name="role"/>
                                    <field name="phone" widget="phone" options="{'enable_sms': False}"/>
                                    <field name="notes"/>
                                </list>
                            </field>
                        </page>
                        <page name="services" string="Services" groups="legal_department_management.group_ldm_government">
                            <field name="template_ids" readonly="1">
                                <list>
                                    <field name="name"/>
                                    <field name="duration_days"/>
                                </list>
                            </field>
                        </page>
                        <page name="letters" string="Letters">
                            <group>
                                <field name="addressee_title" readonly="1"/>
                                <field name="address" readonly="1"/>
                            </group>
                        </page>
                        <page name="notes" string="Notes">
                            <field name="notes" readonly="1"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_ldm_body_directory" model="ir.actions.act_window">
        <field name="name">Bodies and courts</field>
        <field name="res_model">legal.department</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="search_view_id" ref="view_legal_department_search"/>
        <field name="context">{'create': False, 'edit': False, 'delete': False}</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'kanban', 'view_id': ref('view_ldm_body_directory_kanban')}),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_ldm_body_directory_list')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_ldm_body_directory_form')})]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No bodies or courts in the directory</p>
            <p>A legal manager adds them under Configuration, or loads the Iraqi reference library.</p>
        </field>
    </record>

    <!-- =====================================================================
         Matter types and the services a body offers
         ===================================================================== -->
    <record id="view_ldm_task_template_list" model="ir.ui.view">
        <field name="name">legal.task.template.list</field>
        <field name="model">legal.task.template</field>
        <field name="arch" type="xml">
            <list string="Matter types" sample="1" class="o_ldm_view">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="kind"/>
                <field name="department_id" optional="show"/>
                <field name="duration_days" string="Working days" optional="show" width="110px"/>
                <field name="usage_count" string="Matters" optional="show" width="80px"/>
                <field name="track_coverage" optional="hide"/>
                <field name="requires_approval" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_task_template_form" model="ir.ui.view">
        <field name="name">legal.task.template.form</field>
        <field name="model">legal.task.template</field>
        <field name="arch" type="xml">
            <form string="Matter type" class="o_ldm_view">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_tasks" type="object" class="oe_stat_button" icon="fa-folder-open">
                            <field name="usage_count" string="Matters" widget="statinfo"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <field name="active" invisible="1"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="e.g. Tax clearance for companies"/></h1>
                    </div>
                    <group>
                        <group name="what">
                            <field name="kind"/>
                            <field name="request_type" groups="legal_department_management.group_ldm_requests"/>
                            <field name="department_id" options="{'no_quick_create': True}"/>
                            <field name="law_branch" invisible="kind not in ('litigation', 'execution')"/>
                            <field name="lawyer_id" widget="many2one_avatar_user"/>
                        </group>
                        <group name="when">
                            <field name="duration_days"/>
                            <field name="target_days" invisible="kind != 'government'"
                                   placeholder="The body's usual answer time"/>
                            <field name="requires_approval" groups="legal_department_management.group_ldm_approvals"/>
                            <field name="track_coverage" groups="legal_department_management.group_ldm_corporate"/>
                            <field name="confidential_default"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="steps" string="Steps">
                            <field name="step_ids">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="name"/>
                                    <field name="offset_days" string="Days" width="70px"/>
                                    <field name="offset_from"/>
                                    <field name="is_visit" string="Visit" width="60px"/>
                                    <field name="responsible" optional="show"/>
                                    <field name="document_type_id" optional="hide" options="{'no_create': True}"/>
                                </list>
                            </field>
                        </page>
                        <page name="documents" string="Documents to collect">
                            <field name="document_ids">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="document_type_id" options="{'no_create': True}"/>
                                    <field name="mandatory"/>
                                    <field name="note" optional="show"/>
                                </list>
                            </field>
                        </page>
                        <page name="extra_fields" string="Extra fields">
                            <field name="properties_definition"/>
                        </page>
                        <page name="description" string="Description">
                            <field name="description" placeholder="When to use this type, what the body asks for..."/>
                            <group>
                                <field name="fee_amount" invisible="kind != 'government'"/>
                                <field name="currency_id" groups="base.group_multi_currency"/>
                                <field name="ldm_ref_code" invisible="not ldm_ref_code" readonly="1"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_ldm_task_template_search" model="ir.ui.view">
        <field name="name">legal.task.template.search</field>
        <field name="model">legal.task.template</field>
        <field name="arch" type="xml">
            <search string="Matter types">
                <field name="name"/>
                <field name="department_id"/>
                <filter string="Government services" name="filter_government" domain="[('kind', '=', 'government')]"/>
                <filter string="Lawsuits" name="filter_litigation" domain="[('kind', 'in', ('litigation', 'execution'))]"/>
                <filter string="Tracked for every company" name="filter_coverage" domain="[('track_coverage', '=', True)]"/>
                <separator/>
                <filter string="Archived" name="filter_archived" domain="[('active', '=', False)]"/>
                <group>
                    <filter string="Kind" name="group_kind" context="{'group_by': 'kind'}"/>
                    <filter string="Body" name="group_department" context="{'group_by': 'department_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- =====================================================================
         Document types
         ===================================================================== -->
    <record id="view_ldm_document_type_list" model="ir.ui.view">
        <field name="name">legal.document.type.list</field>
        <field name="model">legal.document.type</field>
        <field name="arch" type="xml">
            <list string="Document types" editable="bottom" class="o_ldm_view">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="category"/>
                <field name="validity"/>
                <field name="validity_days" invisible="validity not in ('fixed_days', 'freshness_days')"/>
                <field name="code" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_document_type_form" model="ir.ui.view">
        <field name="name">legal.document.type.form</field>
        <field name="model">legal.document.type</field>
        <field name="arch" type="xml">
            <form string="Document type" class="o_ldm_view">
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <field name="active" invisible="1"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="e.g. Tax clearance"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="category"/>
                            <field name="code"/>
                        </group>
                        <group>
                            <field name="validity"/>
                            <field name="validity_days" invisible="validity not in ('fixed_days', 'freshness_days')"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_ldm_document_type_search" model="ir.ui.view">
        <field name="name">legal.document.type.search</field>
        <field name="model">legal.document.type</field>
        <field name="arch" type="xml">
            <search string="Document types">
                <field name="name"/>
                <field name="code"/>
                <filter string="Expire" name="filter_expire" domain="[('validity', '!=', 'none')]"/>
                <separator/>
                <filter string="Archived" name="filter_archived" domain="[('active', '=', False)]"/>
                <group>
                    <filter string="Category" name="group_category" context="{'group_by': 'category'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- =====================================================================
         The counter-visit dialog (phone first: few fields, big targets)
         ===================================================================== -->
    <record id="view_legal_visit_wizard_form" model="ir.ui.view">
        <field name="name">legal.visit.wizard.form</field>
        <field name="model">legal.visit.wizard</field>
        <field name="arch" type="xml">
            <form string="Log a visit" class="o_ldm_view o_ldm_visit_dialog">
                <field name="task_id" invisible="1"/>
                <field name="step_id" invisible="1"/>
                <field name="currency_id" invisible="1"/>
                <div class="o_ldm_visit_head mb-3">
                    <div class="fw-bold">
                        <field name="visit_name" readonly="step_id" class="w-100" placeholder="What the visit was for"/>
                    </div>
                    <div class="text-muted">
                        <field name="department_id" readonly="step_id" options="{'no_create': True}"
                               placeholder="Body"/>
                    </div>
                </div>
                <field name="result" widget="selection_badge" class="o_ldm_visit_result mb-3"/>
                <group name="paid">
                    <field name="fee_amount" options="{'hide_trailing_zeros': True}"/>
                    <field name="receipt_number" placeholder="Printed on the receipt"/>
                    <field name="photo" widget="image" filename="photo_name"
                           options="{'accepted_file_extensions': 'image/*', 'size': [0, 120]}"/>
                    <field name="photo_name" invisible="1"/>
                </group>
                <group name="next">
                    <field name="next_step" invisible="result != 'done'"
                           placeholder="e.g. Collect the clearance letter"/>
                    <field name="next_date"/>
                    <field name="waiting_for" invisible="result == 'done'"
                           placeholder="e.g. the renewed tax card"/>
                </group>
                <details class="o_ldm_visit_more">
                    <summary class="text-muted">More</summary>
                    <group>
                        <field name="waiting_on" widget="radio" options="{'horizontal': true}"/>
                        <field name="visit_date"/>
                        <field name="currency_id" groups="base.group_multi_currency" options="{'no_create': True}"/>
                        <field name="note"/>
                    </group>
                </details>
                <footer>
                    <button name="action_confirm" type="object" string="Save visit" class="btn-primary" data-hotkey="q"/>
                    <button string="Cancel" special="cancel" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- =====================================================================
         The matter: documents to collect, visits, time at the body
         ===================================================================== -->
    <record id="view_legal_task_form_gov" model="ir.ui.view">
        <field name="name">legal.task.form.gov</field>
        <field name="model">legal.task</field>
        <field name="inherit_id" ref="legal_department_management.view_legal_task_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='action_set_done']" position="before">
                <button name="action_ldm_log_visit" type="object" string="Log visit" class="o_ldm_more"
                        invisible="kind not in ('government', 'execution') or state not in ('in_progress', 'pending_docs')"
                        groups="legal_department_management.group_ldm_clerk"/>
            </xpath>
            <xpath expr="//field[@name='waiting_on']" position="after">
                <field name="ldm_past_target" invisible="1"/>
                <field name="days_at_body" invisible="kind != 'government' or waiting_on not in ('body', 'verification')"/>
                <field name="ldm_body_due_date" widget="remaining_days" invisible="not ldm_body_due_date"/>
            </xpath>
            <xpath expr="//field[@name='step_ids']/list/field[@name='note']" position="after">
                <field name="is_visit" column_invisible="1"/>
                <field name="receipt_number" optional="hide"/>
                <field name="fee_amount" optional="hide" options="{'hide_trailing_zeros': True}"/>
                <field name="visit_result" optional="hide"/>
                <button name="action_ldm_log_visit" type="object" string="Log visit" icon="fa-map-marker"
                        class="btn-link" invisible="not is_visit or state != 'todo'"
                        groups="legal_department_management.group_ldm_clerk"/>
            </xpath>
            <xpath expr="//field[@name='document_ids']/list" position="before">
                <kanban>
                    <field name="name"/>
                    <field name="state"/>
                    <field name="expiry_date"/>
                    <field name="mandatory"/>
                    <field name="file_name"/>
                    <templates>
                        <t t-name="card" class="flex-row align-items-center gap-2">
                            <div class="flex-grow-1 text-truncate">
                                <field name="name" class="fw-bold"/>
                                <div class="small text-muted" t-if="record.expiry_date.raw_value">
                                    <field name="expiry_date" widget="remaining_days"/>
                                </div>
                            </div>
                            <field name="state" widget="ldm_pill"
                                   decoration-danger="state in ('missing', 'expired')"
                                   decoration-warning="state == 'awaiting_verification'"
                                   decoration-success="state in ('received', 'verified')"/>
                        </t>
                    </templates>
                </kanban>
            </xpath>
            <xpath expr="//field[@name='document_ids']/list" position="replace">
                <list editable="bottom" decoration-muted="state == 'not_needed'">
                    <field name="sequence" widget="handle"/>
                    <field name="name"/>
                    <field name="document_type_id" optional="hide" options="{'no_create': True}"/>
                    <field name="mandatory" optional="show" width="100px"/>
                    <field name="state" widget="ldm_pill" width="150px"
                           decoration-danger="state in ('missing', 'expired')"
                           decoration-warning="state == 'awaiting_verification'"
                           decoration-success="state in ('received', 'verified')"/>
                    <field name="expiry_date" widget="remaining_days" optional="show"/>
                    <field name="file_data" filename="file_name" widget="binary" optional="show"/>
                    <field name="file_name" column_invisible="1"/>
                    <field name="received_date" optional="hide"/>
                    <field name="verification_ref" optional="hide"/>
                    <field name="company_document_id" optional="hide" readonly="1"/>
                    <field name="note" optional="hide"/>
                    <field name="ldm_vault_on" column_invisible="1"/>
                    <field name="ldm_in_vault" column_invisible="1"/>
                    <button name="action_ldm_take_from_vault" type="object" icon="fa-archive"
                            title="Take the copy held in the client's records"
                            invisible="not ldm_in_vault or state not in ('missing', 'expired')"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_request_verification" type="object" icon="fa-question-circle"
                            title="Ask the issuing body to confirm it is genuine"
                            invisible="state != 'received'"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_mark_verified" type="object" icon="fa-check-circle"
                            title="The issuing body confirmed it is genuine"
                            invisible="state != 'awaiting_verification'"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_save_to_vault" type="object" icon="fa-folder"
                            title="File it in the client's records for the next matter"
                            invisible="not ldm_vault_on or company_document_id or not document_type_id or state not in ('received', 'awaiting_verification', 'verified')"
                            groups="legal_department_management.group_legal_user"/>
                </list>
            </xpath>
        </field>
    </record>

    <record id="view_ldm_task_document_form" model="ir.ui.view">
        <field name="name">legal.task.document.form</field>
        <field name="model">legal.task.document</field>
        <field name="arch" type="xml">
            <form string="Document to collect" class="o_ldm_view">
                <field name="ldm_vault_on" invisible="1"/>
                <field name="ldm_in_vault" invisible="1"/>
                <field name="company_document_id" invisible="1"/>
                <header>
                    <button name="action_ldm_take_from_vault" type="object" string="Take from the client's records"
                            invisible="not ldm_in_vault or state not in ('missing', 'expired')"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_request_verification" type="object" string="Ask for confirmation"
                            invisible="state != 'received'" groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_mark_verified" type="object" string="Confirmed genuine"
                            invisible="state != 'awaiting_verification'"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <field name="state" widget="statusbar" statusbar_visible="missing,received,verified"
                           options="{'clickable': '1'}"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h2><field name="name"/></h2>
                    </div>
                    <group>
                        <group>
                            <field name="document_type_id" options="{'no_create': True}"/>
                            <field name="mandatory"/>
                            <field name="file_data" filename="file_name" widget="binary"/>
                            <field name="file_name" invisible="1"/>
                        </group>
                        <group>
                            <field name="received_date"/>
                            <field name="expiry_date"/>
                            <field name="verification_ref" invisible="state not in ('awaiting_verification', 'verified')"/>
                            <field name="verification_date" invisible="state != 'verified'"/>
                            <field name="original_held"/>
                        </group>
                    </group>
                    <field name="note" placeholder="What the body said about this document..."/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_legal_task_search_gov" model="ir.ui.view">
        <field name="name">legal.task.search.gov</field>
        <field name="model">legal.task</field>
        <field name="inherit_id" ref="legal_department_management.view_legal_task_search"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='filter_at_body']" position="after">
                <filter string="At the body past target" name="filter_past_target"
                        domain="[('ldm_past_target', '=', True)]"
                        groups="legal_department_management.group_ldm_government"/>
            </xpath>
        </field>
    </record>

    <!-- =====================================================================
         Company records: document vault, obligations, coverage (dossier)
         ===================================================================== -->
    <record id="view_legal_company_form_gov" model="ir.ui.view">
        <field name="name">legal.company.form.gov</field>
        <field name="model">legal.company</field>
        <field name="inherit_id" ref="legal_department_management.view_legal_company_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_vault" type="object" class="oe_stat_button" icon="fa-calendar-times-o"
                        invisible="not expiring_document_count"
                        groups="legal_department_management.group_ldm_corporate">
                    <field name="expiring_document_count" widget="statinfo" string="Expiring documents"/>
                </button>
            </xpath>
            <xpath expr="//page[@name='registry']" position="after">
                <page name="vault" string="Documents" groups="legal_department_management.group_ldm_corporate">
                    <div class="d-flex justify-content-end mb-2">
                        <button name="action_ldm_readiness" type="object" string="Valid on a date"
                                icon="fa-calendar-check-o" class="btn-secondary"/>
                    </div>
                    <field name="document_ids" context="{'default_legal_company_id': id}">
                        <kanban>
                            <field name="document_type_id"/>
                            <field name="number"/>
                            <field name="date_expiry"/>
                            <field name="state"/>
                            <templates>
                                <t t-name="card" class="flex-row align-items-center gap-2">
                                    <div class="flex-grow-1 text-truncate">
                                        <field name="document_type_id" class="fw-bold"/>
                                        <div class="small text-muted">
                                            <field name="number"/>
                                            <span t-if="record.date_expiry.raw_value"> · <field name="date_expiry"/></span>
                                        </div>
                                    </div>
                                    <field name="state" widget="ldm_pill"
                                           decoration-success="state == 'valid'"
                                           decoration-warning="state == 'expiring'"
                                           decoration-danger="state == 'expired'"/>
                                </t>
                            </templates>
                        </kanban>
                        <list editable="bottom" decoration-danger="state == 'expired'"
                              decoration-warning="state == 'expiring'">
                            <field name="document_type_id" options="{'no_create': True}"/>
                            <field name="number"/>
                            <field name="date_issued" optional="show"/>
                            <field name="date_expiry" optional="show"/>
                            <field name="state" widget="ldm_pill" width="120px"
                                   decoration-success="state == 'valid'"
                                   decoration-warning="state == 'expiring'"
                                   decoration-danger="state == 'expired'"/>
                            <field name="file_data" filename="file_name" widget="binary" optional="show"/>
                            <field name="file_name" column_invisible="1"/>
                            <field name="confidential" optional="hide"/>
                        </list>
                    </field>
                </page>
                <page name="obligations" string="Obligations" groups="legal_department_management.group_ldm_corporate">
                    <field name="obligation_ids" context="{'default_legal_company_id': id}">
                        <list editable="bottom">
                            <field name="name"/>
                            <field name="template_id" options="{'no_create': True}" optional="show"/>
                            <field name="recurrence"/>
                            <field name="month" invisible="recurrence == 'monthly'" optional="show"/>
                            <field name="day" optional="show"/>
                            <field name="lead_days" optional="hide"/>
                            <field name="next_date" widget="remaining_days"/>
                            <field name="last_task_id" readonly="1" optional="hide"/>
                        </list>
                    </field>
                </page>
                <page name="coverage" string="Services this year" groups="legal_department_management.group_ldm_corporate"
                      invisible="not coverage_ids">
                    <field name="coverage_ids" readonly="1">
                        <list create="0" delete="0" decoration-muted="coverage_state == 'done'">
                            <field name="template_id"/>
                            <field name="department_id" optional="hide"/>
                            <field name="coverage_state" widget="ldm_pill" width="150px"
                                   decoration-success="coverage_state == 'done'"
                                   decoration-info="coverage_state == 'open'"
                                   decoration-danger="coverage_state == 'due'"/>
                            <field name="last_done_date" optional="show"/>
                            <field name="last_task_id" optional="hide"/>
                            <button name="action_ldm_start" type="object" string="Start" icon="fa-play"
                                    class="btn-link" invisible="coverage_state != 'due'"
                                    groups="legal_department_management.group_ldm_clerk"/>
                            <button name="action_open_last_task" type="object" string="Open" icon="fa-folder-open-o"
                                    class="btn-link" invisible="coverage_state == 'due' or not last_task_id"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <!-- Company documents register -->
    <record id="action_ldm_readiness_wizard" model="ir.actions.act_window">
        <field name="name">Documents valid on a date</field>
        <field name="res_model">legal.readiness.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <record id="view_ldm_company_document_list" model="ir.ui.view">
        <field name="name">legal.company.document.list</field>
        <field name="model">legal.company.document</field>
        <field name="arch" type="xml">
            <list string="Company documents" sample="1" default_order="date_expiry"
                  decoration-danger="state == 'expired'" decoration-warning="state == 'expiring'" class="o_ldm_view">
                <header>
                    <button name="%(action_ldm_readiness_wizard)d" type="action" string="Valid on a date"
                            display="always"/>
                </header>
                <field name="legal_company_id"/>
                <field name="document_type_id"/>
                <field name="number" optional="show"/>
                <field name="date_expiry" widget="remaining_days" optional="show"/>
                <field name="state" widget="ldm_pill"
                       decoration-success="state == 'valid'"
                       decoration-warning="state == 'expiring'"
                       decoration-danger="state == 'expired'"/>
                <field name="attachment_id" optional="show" string="File"/>
                <field name="date_issued" optional="hide"/>
                <field name="category" optional="hide"/>
                <field name="confidential" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_company_document_form" model="ir.ui.view">
        <field name="name">legal.company.document.form</field>
        <field name="model">legal.company.document</field>
        <field name="arch" type="xml">
            <form string="Company document" class="o_ldm_view">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_open_deadline" type="object" class="oe_stat_button" icon="fa-hourglass-half"
                                invisible="not deadline_id" string="Expiry deadline"/>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <field name="active" invisible="1"/>
                    <field name="deadline_id" invisible="1"/>
                    <div class="oe_title">
                        <label for="document_type_id"/>
                        <h1><field name="document_type_id" options="{'no_create': True}" placeholder="e.g. Tax clearance"/></h1>
                    </div>
                    <group>
                        <group name="what">
                            <field name="legal_company_id" options="{'no_create': True}"/>
                            <field name="number"/>
                            <field name="name" string="Note"/>
                            <field name="confidential"/>
                            <field name="original_held"/>
                        </group>
                        <group name="when">
                            <field name="date_issued"/>
                            <field name="date_expiry"/>
                            <field name="state" widget="ldm_pill"
                                   decoration-success="state == 'valid'"
                                   decoration-warning="state == 'expiring'"
                                   decoration-danger="state == 'expired'"/>
                            <field name="validity" invisible="1"/>
                        </group>
                    </group>
                    <group>
                        <field name="file_data" filename="file_name" widget="binary"/>
                        <field name="file_name" invisible="1"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_company_document_search" model="ir.ui.view">
        <field name="name">legal.company.document.search</field>
        <field name="model">legal.company.document</field>
        <field name="arch" type="xml">
            <search string="Company documents">
                <field name="legal_company_id"/>
                <field name="document_type_id"/>
                <field name="number"/>
                <filter string="Expiring or expired" name="filter_attention" domain="[('state', 'in', ('expiring', 'expired'))]"/>
                <filter string="Valid" name="filter_valid" domain="[('state', '=', 'valid')]"/>
                <separator/>
                <filter string="Confidential" name="filter_confidential" domain="[('confidential', '=', True)]"/>
                <filter string="Archived" name="filter_archived" domain="[('active', '=', False)]"/>
                <group>
                    <filter string="Client" name="group_client" context="{'group_by': 'legal_company_id'}"/>
                    <filter string="Document type" name="group_type" context="{'group_by': 'document_type_id'}"/>
                    <filter string="Category" name="group_category" context="{'group_by': 'category'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_company_document" model="ir.actions.act_window">
        <field name="name">Company documents</field>
        <field name="res_model">legal.company.document</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ldm_company_document_search"/>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No company documents yet</p>
            <p>File each client's registration certificate, tax clearance, chamber ID and powers of attorney once, with their expiry dates. New matters take them from here, and you are reminded before they expire.</p>
        </field>
    </record>

    <!-- Recurring obligations -->
    <record id="view_ldm_obligation_list" model="ir.ui.view">
        <field name="name">legal.obligation.list</field>
        <field name="model">legal.obligation</field>
        <field name="arch" type="xml">
            <list string="Recurring obligations" sample="1" class="o_ldm_view">
                <field name="name"/>
                <field name="legal_company_id"/>
                <field name="template_id" optional="show"/>
                <field name="recurrence" optional="show"/>
                <field name="next_date" widget="remaining_days"/>
                <field name="open_on" optional="show"/>
                <field name="last_task_id" optional="hide"/>
                <field name="lead_days" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_obligation_form" model="ir.ui.view">
        <field name="name">legal.obligation.form</field>
        <field name="model">legal.obligation</field>
        <field name="arch" type="xml">
            <form string="Recurring obligation" class="o_ldm_view">
                <header>
                    <button name="action_ldm_open_now" type="object" string="Open the matter now"
                            invisible="not template_id or not next_date or not id"
                            groups="legal_department_management.group_legal_user"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_tasks" type="object" class="oe_stat_button" icon="fa-folder-open">
                            <field name="task_count" string="Matters" widget="statinfo"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <field name="active" invisible="1"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="e.g. Annual tax return"/></h1>
                    </div>
                    <group>
                        <group name="what">
                            <field name="legal_company_id" options="{'no_create': True}"/>
                            <field name="template_id" options="{'no_create': True}"
                                   placeholder="Leave empty to be reminded only"/>
                            <field name="department_id" invisible="not department_id"/>
                            <field name="lawyer_id" widget="many2one_avatar_user"
                                   placeholder="The client's responsible"/>
                        </group>
                        <group name="when">
                            <field name="recurrence" widget="radio" options="{'horizontal': true}"/>
                            <field name="month" invisible="recurrence == 'monthly'"/>
                            <field name="day"/>
                            <field name="lead_days"/>
                            <field name="next_date"/>
                            <field name="open_on" invisible="not open_on or not template_id"/>
                            <field name="last_task_id" invisible="not last_task_id"/>
                        </group>
                    </group>
                    <field name="note" placeholder="Where it is filed, who signs, penalties for lateness..."/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_ldm_obligation_search" model="ir.ui.view">
        <field name="name">legal.obligation.search</field>
        <field name="model">legal.obligation</field>
        <field name="arch" type="xml">
            <search string="Recurring obligations">
                <field name="name"/>
                <field name="legal_company_id"/>
                <field name="template_id"/>
                <filter string="Due in 30 days" name="filter_due_soon"
                        domain="[('next_date', '&lt;=', (context_today() + relativedelta(days=30)).strftime('%Y-%m-%d'))]"/>
                <filter string="Reminder only" name="filter_reminder_only" domain="[('template_id', '=', False)]"/>
                <separator/>
                <filter string="Archived" name="filter_archived" domain="[('active', '=', False)]"/>
                <group>
                    <filter string="Client" name="group_client" context="{'group_by': 'legal_company_id'}"/>
                    <filter string="Repeats" name="group_recurrence" context="{'group_by': 'recurrence'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_obligation" model="ir.actions.act_window">
        <field name="name">Recurring obligations</field>
        <field name="res_model">legal.obligation</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ldm_obligation_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No recurring obligations yet</p>
            <p>Add what each client must file every month or year, such as the annual tax return or the chamber ID renewal. The matter opens by itself before the due date.</p>
        </field>
    </record>

    <!-- Coverage: which tracked services each client has done this year -->
    <record id="view_ldm_company_coverage_list" model="ir.ui.view">
        <field name="name">legal.company.coverage.list</field>
        <field name="model">legal.company.coverage</field>
        <field name="arch" type="xml">
            <list string="Services this year" create="0" edit="0" delete="0" expand="1"
                  decoration-muted="coverage_state == 'done'" class="o_ldm_view">
                <field name="legal_company_id"/>
                <field name="template_id"/>
                <field name="department_id" optional="show"/>
                <field name="coverage_state" widget="ldm_pill" width="150px"
                       decoration-success="coverage_state == 'done'"
                       decoration-info="coverage_state == 'open'"
                       decoration-danger="coverage_state == 'due'"/>
                <field name="last_done_date" optional="show"/>
                <field name="last_task_id" optional="hide"/>
                <button name="action_ldm_start" type="object" string="Start" icon="fa-play" class="btn-link"
                        invisible="coverage_state != 'due'" groups="legal_department_management.group_ldm_clerk"/>
                <button name="action_open_last_task" type="object" string="Open" icon="fa-folder-open-o" class="btn-link"
                        invisible="coverage_state == 'due' or not last_task_id"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_company_coverage_search" model="ir.ui.view">
        <field name="name">legal.company.coverage.search</field>
        <field name="model">legal.company.coverage</field>
        <field name="arch" type="xml">
            <search string="Services this year">
                <field name="legal_company_id"/>
                <field name="template_id"/>
                <field name="department_id"/>
                <filter string="Not done this year" name="filter_due" domain="[('coverage_state', '=', 'due')]"/>
                <filter string="In progress" name="filter_open" domain="[('coverage_state', '=', 'open')]"/>
                <filter string="Done this year" name="filter_done" domain="[('coverage_state', '=', 'done')]"/>
                <group>
                    <filter string="Client" name="group_client" context="{'group_by': 'legal_company_id'}"/>
                    <filter string="Service" name="group_service" context="{'group_by': 'template_id'}"/>
                    <filter string="Body" name="group_body" context="{'group_by': 'department_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_company_coverage" model="ir.actions.act_window">
        <field name="name">Services this year</field>
        <field name="res_model">legal.company.coverage</field>
        <field name="view_mode">list</field>
        <field name="search_view_id" ref="view_ldm_company_coverage_search"/>
        <field name="context">{'search_default_group_client': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No service is tracked for every company</p>
            <p>Tick "Track for every company" on the matter types a legal manager wants followed yearly, such as the tax clearance or the chamber ID renewal.</p>
        </field>
    </record>

    <menuitem id="menu_ldm_coverage" name="Services this year" parent="menu_ldm_registers" sequence="6"
              action="action_ldm_company_coverage" groups="legal_department_management.group_ldm_corporate"/>

    <!-- Readiness at a date -->
    <record id="view_legal_readiness_wizard_form" model="ir.ui.view">
        <field name="name">legal.readiness.wizard.form</field>
        <field name="model">legal.readiness.wizard</field>
        <field name="arch" type="xml">
            <form string="Documents valid on a date" class="o_ldm_view">
                <field name="ready_count" invisible="1"/>
                <field name="total_count" invisible="1"/>
                <group>
                    <group>
                        <field name="legal_company_id" options="{'no_create': True}"/>
                        <field name="template_id" options="{'no_create': True}"
                               placeholder="All document types the client holds"/>
                    </group>
                    <group>
                        <field name="date"/>
                    </group>
                </group>
                <div class="alert alert-success mb-2" role="status"
                     invisible="not total_count or ready_count != total_count">
                    <field name="summary" class="d-inline"/>
                </div>
                <div class="alert alert-warning mb-2" role="status"
                     invisible="not summary or (total_count and ready_count == total_count)">
                    <field name="summary" class="d-inline"/>
                </div>
                <field name="line_ids">
                    <list create="0" delete="0" edit="0"
                          decoration-success="readiness == 'valid'"
                          decoration-warning="readiness == 'expires'"
                          decoration-danger="readiness == 'missing' and mandatory">
                        <field name="document_type_id"/>
                        <field name="mandatory" optional="hide"/>
                        <field name="number"/>
                        <field name="company_document_id" optional="hide"/>
                        <field name="date_expiry"/>
                        <field name="readiness" widget="ldm_pill"
                               decoration-success="readiness == 'valid'"
                               decoration-warning="readiness == 'expires'"
                               decoration-danger="readiness == 'missing'"/>
                    </list>
                </field>
                <footer>
                    <button string="Close" special="cancel" class="btn-primary" data-hotkey="x"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>
