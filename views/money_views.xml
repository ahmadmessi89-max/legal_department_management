<?xml version="1.0" encoding="utf-8"?>
<!-- Owned by the money stream (docs/ldm/SPEC.md section 11). -->
<odoo>
    <!-- ================================================================
         Expense categories
         ================================================================ -->
    <record id="view_ldm_expense_category_list" model="ir.ui.view">
        <field name="name">legal.expense.category.list</field>
        <field name="model">legal.expense.category</field>
        <field name="arch" type="xml">
            <list string="Expense categories" editable="bottom" class="o_ldm_view">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="code" optional="show"/>
                <field name="recoverable_default"/>
                <field name="account_id" optional="show" groups="account.group_account_invoice,account.group_account_readonly"/>
                <field name="active" column_invisible="1"/>
            </list>
        </field>
    </record>

    <record id="action_ldm_expense_category" model="ir.actions.act_window">
        <field name="name">Expense categories</field>
        <field name="res_model">legal.expense.category</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_ldm_expense_category_list"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No expense categories</p>
            <p>Add the kinds of fees and costs you pay on matters: court fees, stamps, expert deposits, publication.</p>
        </field>
    </record>

    <!-- ================================================================
         Expenses
         ================================================================ -->
    <record id="view_ldm_expense_list" model="ir.ui.view">
        <field name="name">legal.task.expense.list</field>
        <field name="model">legal.task.expense</field>
        <field name="arch" type="xml">
            <list string="Expenses" sample="1" class="o_ldm_view">
                <field name="date"/>
                <field name="task_id"/>
                <field name="category_id" optional="show"/>
                <field name="name"/>
                <field name="amount" sum="Total"/>
                <field name="paid_by" optional="show"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="receipt_number" optional="hide"/>
                <field name="legal_company_id" optional="hide"/>
                <field name="recoverable" optional="hide" groups="legal_department_management.group_ldm_billing"/>
                <field name="billed" optional="hide" groups="legal_department_management.group_ldm_billing"/>
                <field name="employee_user_id" optional="hide" widget="many2one_avatar_user"/>
                <field name="advance_id" optional="hide"/>
                <field name="state" optional="hide" widget="ldm_pill" decoration-muted="state == 'draft'"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_expense_form" model="ir.ui.view">
        <field name="name">legal.task.expense.form</field>
        <field name="model">legal.task.expense</field>
        <field name="arch" type="xml">
            <form string="Expense" class="o_ldm_view">
                <field name="company_id" invisible="1"/>
                <field name="ldm_can_post" invisible="1"/>
                <field name="ldm_posted" invisible="1"/>
                <field name="billed" invisible="1"/>
                <header>
                    <button name="action_ldm_post" type="object" string="Post to accounting" class="btn-secondary"
                            invisible="not ldm_can_post" groups="legal_department_management.group_ldm_accounting_links"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="alert alert-info mb-3" role="status" invisible="not billed">
                        This expense is on an invoice to the client.
                    </div>
                    <group>
                        <group>
                            <field name="task_id" options="{'no_create': True}"/>
                            <field name="category_id" options="{'no_create': True}"/>
                            <field name="name" placeholder="e.g. Court fee for the statement of claim"/>
                            <label for="amount"/>
                            <div class="d-flex gap-2">
                                <field name="amount" class="oe_inline"/>
                                <field name="currency_id" class="oe_inline" options="{'no_create': True}"
                                       groups="base.group_multi_currency"/>
                            </div>
                        </group>
                        <group>
                            <field name="date"/>
                            <field name="receipt_number" placeholder="Number on the government receipt"/>
                            <field name="paid_by" widget="radio"/>
                            <field name="employee_user_id" invisible="paid_by != 'employee'" widget="many2one_avatar_user"/>
                            <field name="advance_id" invisible="paid_by != 'employee'"
                                   domain="[('user_id', '=', employee_user_id), ('state', '=', 'paid'), ('currency_id', '=', currency_id)]"
                                   options="{'no_create': True}"/>
                            <field name="fund_line_id" invisible="not fund_line_id"/>
                            <field name="recoverable" groups="legal_department_management.group_ldm_billing"
                                   invisible="paid_by in ('client', 'client_funds')"/>
                        </group>
                    </group>
                    <group name="accounting" string="Accounting" invisible="not ldm_posted"
                           groups="account.group_account_invoice,account.group_account_readonly">
                        <field name="move_id" invisible="not move_id"/>
                        <field name="payment_id" invisible="not payment_id"/>
                        <field name="invoice_line_id" invisible="not invoice_line_id"/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_expense_search" model="ir.ui.view">
        <field name="name">legal.task.expense.search</field>
        <field name="model">legal.task.expense</field>
        <field name="arch" type="xml">
            <search string="Expenses">
                <field name="name"/>
                <field name="task_id"/>
                <field name="legal_company_id"/>
                <field name="receipt_number"/>
                <field name="category_id"/>
                <field name="employee_user_id"/>
                <filter string="Paid by me" name="filter_mine" domain="[('employee_user_id', '=', uid)]"/>
                <separator/>
                <filter string="Recharged to the client" name="filter_recoverable" domain="[('recoverable', '=', True)]"
                        groups="legal_department_management.group_ldm_billing"/>
                <filter string="Not invoiced" name="filter_unbilled" domain="[('recoverable', '=', True), ('billed', '=', False)]"
                        groups="legal_department_management.group_ldm_billing"/>
                <filter string="From client money" name="filter_client_funds" domain="[('paid_by', '=', 'client_funds')]"
                        groups="legal_department_management.group_ldm_billing"/>
                <filter string="From a cash advance" name="filter_advance" domain="[('paid_by', '=', 'employee')]"/>
                <separator/>
                <filter string="Date" name="filter_date" date="date"/>
                <group>
                    <filter string="Matter" name="group_task" context="{'group_by': 'task_id'}"/>
                    <filter string="Client" name="group_client" context="{'group_by': 'legal_company_id'}"/>
                    <filter string="Category" name="group_category" context="{'group_by': 'category_id'}"/>
                    <filter string="Paid by" name="group_paid_by" context="{'group_by': 'paid_by'}"/>
                    <filter string="Month" name="group_month" context="{'group_by': 'date:month'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="view_ldm_expense_pivot" model="ir.ui.view">
        <field name="name">legal.task.expense.pivot</field>
        <field name="model">legal.task.expense</field>
        <field name="arch" type="xml">
            <pivot string="Expenses analysis" sample="1" class="o_ldm_view">
                <field name="category_id" type="row"/>
                <field name="date" interval="month" type="col"/>
                <field name="amount_company" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="view_ldm_expense_graph" model="ir.ui.view">
        <field name="name">legal.task.expense.graph</field>
        <field name="model">legal.task.expense</field>
        <field name="arch" type="xml">
            <graph string="Expenses analysis" type="bar" sample="1" class="o_ldm_view">
                <field name="date" interval="month"/>
                <field name="amount_company" type="measure"/>
            </graph>
        </field>
    </record>

    <!-- Runners record and check expenses on their phones: cards there. -->
    <record id="view_ldm_expense_kanban" model="ir.ui.view">
        <field name="name">legal.task.expense.kanban</field>
        <field name="model">legal.task.expense</field>
        <field name="arch" type="xml">
            <kanban class="o_ldm_view o_ldm_kanban" sample="1">
                <field name="name"/>
                <field name="date"/>
                <field name="task_id"/>
                <field name="category_id"/>
                <field name="amount"/>
                <field name="currency_id"/>
                <field name="paid_by"/>
                <field name="receipt_number"/>
                <templates>
                    <t t-name="card">
                        <div class="d-flex justify-content-between gap-2">
                            <field name="name" class="fw-bold text-truncate"/>
                            <field name="amount" widget="monetary" class="text-nowrap"/>
                        </div>
                        <div class="text-muted small text-truncate"><field name="task_id"/></div>
                        <div class="small text-muted text-truncate">
                            <field name="date"/> · <field name="category_id"/>
                        </div>
                        <div class="small text-truncate">
                            <field name="paid_by"/>
                            <t t-if="record.receipt_number.raw_value"> · <field name="receipt_number"/></t>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="action_ldm_expense" model="ir.actions.act_window">
        <field name="name">Expenses</field>
        <field name="res_model">legal.task.expense</field>
        <field name="view_mode">list,kanban,form,pivot</field>
        <field name="mobile_view_mode">kanban</field>
        <field name="search_view_id" ref="view_ldm_expense_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No expenses recorded</p>
            <p>Record each fee or cost paid on a matter with its receipt number, from the matter's Money tab or here.</p>
        </field>
    </record>

    <record id="action_ldm_expense_analysis" model="ir.actions.act_window">
        <field name="name">Expenses analysis</field>
        <field name="res_model">legal.task.expense</field>
        <field name="view_mode">pivot,graph,list</field>
        <field name="search_view_id" ref="view_ldm_expense_search"/>
    </record>

    <!-- ================================================================
         Cash advances
         ================================================================ -->
    <record id="view_ldm_advance_list" model="ir.ui.view">
        <field name="name">legal.advance.list</field>
        <field name="model">legal.advance</field>
        <field name="arch" type="xml">
            <list string="Cash advances" sample="1" decoration-muted="state == 'settled'" class="o_ldm_view">
                <field name="date"/>
                <field name="user_id" widget="many2one_avatar_user"/>
                <field name="name"/>
                <field name="amount" sum="Total"/>
                <field name="balance" sum="Total" decoration-warning="state == 'paid' and balance &gt; 0"/>
                <field name="state" widget="ldm_pill" decoration-info="state == 'draft'"
                       decoration-warning="state == 'paid'" decoration-success="state == 'settled'"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="task_id" optional="hide"/>
                <field name="spent_amount" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_advance_form" model="ir.ui.view">
        <field name="name">legal.advance.form</field>
        <field name="model">legal.advance</field>
        <field name="arch" type="xml">
            <form string="Cash advance" class="o_ldm_view">
                <field name="ldm_can_handle" invisible="1"/>
                <field name="company_id" invisible="1"/>
                <header>
                    <button name="action_ldm_hand_over" type="object" string="Hand over" class="btn-primary"
                            invisible="state != 'draft' or not ldm_can_handle"/>
                    <button name="action_ldm_settle" type="object" string="Settle" class="btn-primary"
                            invisible="state != 'paid' or not ldm_can_handle"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_expenses" type="object" class="oe_stat_button" icon="fa-file-text-o">
                            <field name="expense_count" widget="statinfo" string="Receipts"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="e.g. Fees for the tax clearance renewals"
                                   readonly="state == 'settled'"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="user_id" widget="many2one_avatar_user" readonly="state != 'draft'"/>
                            <field name="date" readonly="state != 'draft'"/>
                            <field name="task_id" options="{'no_create': True}" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <label for="amount"/>
                            <div class="d-flex gap-2">
                                <field name="amount" class="oe_inline" readonly="state != 'draft'"/>
                                <field name="currency_id" class="oe_inline" options="{'no_create': True}"
                                       groups="base.group_multi_currency" readonly="state != 'draft'"/>
                            </div>
                            <field name="spent_amount"/>
                            <field name="returned_amount" invisible="state != 'settled'"/>
                            <field name="balance" class="fw-bold"/>
                        </group>
                    </group>
                    <field name="expense_ids" readonly="1">
                        <list>
                            <field name="date"/>
                            <field name="task_id"/>
                            <field name="name"/>
                            <field name="receipt_number"/>
                            <field name="amount" sum="Total"/>
                            <field name="currency_id" column_invisible="1"/>
                        </list>
                        <kanban class="o_ldm_kanban">
                            <field name="name"/>
                            <field name="date"/>
                            <field name="task_id"/>
                            <field name="amount"/>
                            <field name="currency_id"/>
                            <field name="receipt_number"/>
                            <templates>
                                <t t-name="card">
                                    <div class="d-flex justify-content-between gap-2">
                                        <field name="name" class="fw-bold text-truncate"/>
                                        <field name="amount" widget="monetary" class="text-nowrap"/>
                                    </div>
                                    <div class="text-muted small text-truncate"><field name="task_id"/></div>
                                    <div class="small text-muted">
                                        <field name="date"/>
                                        <t t-if="record.receipt_number.raw_value"> · <field name="receipt_number"/></t>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                    </field>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_advance_search" model="ir.ui.view">
        <field name="name">legal.advance.search</field>
        <field name="model">legal.advance</field>
        <field name="arch" type="xml">
            <search string="Cash advances">
                <field name="name"/>
                <field name="user_id"/>
                <filter string="Mine" name="filter_mine" domain="[('user_id', '=', uid)]"/>
                <separator/>
                <filter string="Not settled" name="filter_open" domain="[('state', '!=', 'settled')]"/>
                <filter string="Settled" name="filter_settled" domain="[('state', '=', 'settled')]"/>
                <group>
                    <filter string="Given to" name="group_user" context="{'group_by': 'user_id'}"/>
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_advance" model="ir.actions.act_window">
        <field name="name">Cash advances</field>
        <field name="res_model">legal.advance</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ldm_advance_search"/>
        <field name="context">{'search_default_filter_open': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No cash advances</p>
            <p>Record the cash given to a runner for government fees; settle it against the receipts they bring back.</p>
        </field>
    </record>

    <!-- ================================================================
         Client money
         ================================================================ -->
    <record id="view_ldm_client_fund_list" model="ir.ui.view">
        <field name="name">legal.client.fund.line.list</field>
        <field name="model">legal.client.fund.line</field>
        <field name="arch" type="xml">
            <list string="Client money" sample="1" create="0" expand="1" class="o_ldm_view">
                <header>
                    <button name="action_ldm_receive" type="object" string="Receive money" class="btn-primary"
                            display="always"
                            groups="legal_department_management.group_ldm_clerk,legal_department_management.group_ldm_billing_user"/>
                    <button name="action_ldm_pay_out" type="object" string="Pay out" display="always"
                            groups="legal_department_management.group_ldm_clerk,legal_department_management.group_ldm_billing_user"/>
                </header>
                <field name="date"/>
                <field name="legal_company_id"/>
                <field name="kind"/>
                <field name="task_id" optional="show"/>
                <field name="signed_amount" sum="Held" string="Amount"
                       decoration-success="signed_amount &gt; 0" decoration-danger="signed_amount &lt; 0"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="receipt_number" optional="show"/>
                <field name="note" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_client_fund_form" model="ir.ui.view">
        <field name="name">legal.client.fund.line.form</field>
        <field name="model">legal.client.fund.line</field>
        <field name="arch" type="xml">
            <form string="Client money movement" create="0" class="o_ldm_view">
                <sheet>
                    <group>
                        <group>
                            <field name="legal_company_id" options="{'no_create': True}"/>
                            <field name="task_id" options="{'no_create': True}"
                                   domain="[('legal_company_id', '=', legal_company_id)]"/>
                            <field name="kind"/>
                            <label for="amount"/>
                            <div class="d-flex gap-2">
                                <field name="amount" class="oe_inline"/>
                                <field name="currency_id" class="oe_inline" options="{'no_create': True}"
                                       groups="base.group_multi_currency"/>
                            </div>
                        </group>
                        <group>
                            <field name="date"/>
                            <field name="receipt_number"/>
                            <field name="attachment_id" invisible="not attachment_id"/>
                            <field name="note"/>
                            <field name="ldm_client_balance" class="fw-bold"/>
                        </group>
                    </group>
                    <group invisible="not override_reason">
                        <field name="override_reason"/>
                    </group>
                    <field name="company_id" invisible="1"/>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_client_fund_search" model="ir.ui.view">
        <field name="name">legal.client.fund.line.search</field>
        <field name="model">legal.client.fund.line</field>
        <field name="arch" type="xml">
            <search string="Client money">
                <field name="legal_company_id"/>
                <field name="task_id"/>
                <field name="receipt_number"/>
                <filter string="Received" name="filter_in" domain="[('kind', 'in', ('deposit', 'execution_collection'))]"/>
                <filter string="Paid out" name="filter_out" domain="[('kind', 'not in', ('deposit', 'execution_collection'))]"/>
                <separator/>
                <filter string="Date" name="filter_date" date="date"/>
                <group>
                    <filter string="Client and currency" name="group_balance" context="{'group_by': 'balance_key'}"/>
                    <filter string="Client" name="group_client" context="{'group_by': 'legal_company_id'}"/>
                    <filter string="Currency" name="group_currency" context="{'group_by': 'currency_id'}"/>
                    <filter string="Movement" name="group_kind" context="{'group_by': 'kind'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_client_fund" model="ir.actions.act_window">
        <field name="name">Client money</field>
        <field name="res_model">legal.client.fund.line</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ldm_client_fund_search"/>
        <field name="context">{'search_default_group_balance': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No client money held</p>
            <p>Use <b>Receive money</b> when a client hands over money for court fees, experts or deposits; each client's balance stays per currency and never goes below zero.</p>
        </field>
    </record>

    <!-- ================================================================
         Fee agreements
         ================================================================ -->
    <record id="view_ldm_engagement_list" model="ir.ui.view">
        <field name="name">legal.engagement.list</field>
        <field name="model">legal.engagement</field>
        <field name="arch" type="xml">
            <list string="Fee agreements" sample="1" decoration-muted="state == 'closed'" class="o_ldm_view">
                <field name="name"/>
                <field name="legal_company_id"/>
                <field name="fee_type"/>
                <field name="amount" invisible="not amount"/>
                <field name="signed" widget="boolean_toggle" readonly="1" optional="show"/>
                <field name="state" widget="ldm_pill" decoration-info="state == 'draft'" decoration-success="state == 'active'"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="lawyer_id" widget="many2one_avatar_user" optional="hide"/>
                <field name="date_start" optional="hide"/>
                <field name="amount_due" optional="hide"/>
                <field name="amount_planned" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_engagement_kanban" model="ir.ui.view">
        <field name="name">legal.engagement.kanban</field>
        <field name="model">legal.engagement</field>
        <field name="arch" type="xml">
            <kanban default_group_by="state" quick_create="false" class="o_ldm_view o_ldm_kanban" sample="1">
                <field name="name"/>
                <field name="legal_company_id"/>
                <field name="fee_type"/>
                <field name="amount"/>
                <field name="currency_id"/>
                <field name="signed"/>
                <field name="lawyer_id"/>
                <field name="task_count"/>
                <templates>
                    <t t-name="card">
                        <field name="legal_company_id" class="fw-bold text-truncate"/>
                        <div class="text-muted small text-truncate">
                            <field name="fee_type"/> · <field name="amount" widget="monetary"/>
                        </div>
                        <footer class="pt-1">
                            <span class="small" invisible="signed">Not signed</span>
                            <span class="small text-muted" invisible="not signed">Signed</span>
                            <field name="lawyer_id" widget="many2one_avatar_user" class="ms-auto"/>
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_ldm_engagement_form" model="ir.ui.view">
        <field name="name">legal.engagement.form</field>
        <field name="model">legal.engagement</field>
        <field name="arch" type="xml">
            <form string="Fee agreement" class="o_ldm_view">
                <field name="ldm_is_manager" invisible="1"/>
                <field name="ldm_can_message" invisible="1"/>
                <field name="company_id" invisible="1"/>
                <field name="company_currency_id" invisible="1"/>
                <field name="cap_exceeded" invisible="1"/>
                <field name="cap_applies" invisible="1"/>
                <field name="retainer_below_minimum" invisible="1"/>
                <header>
                    <button name="action_ldm_activate" type="object" string="Activate" class="btn-primary"
                            invisible="state != 'draft' or (cap_exceeded and not ldm_is_manager)"
                            groups="legal_department_management.group_legal_user,legal_department_management.group_ldm_billing_user"/>
                    <button name="action_ldm_invoice" type="object" string="Create invoice" class="btn-primary"
                            invisible="state != 'active' or not amount_due" groups="account.group_account_invoice"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,closed"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_tasks" type="object" class="oe_stat_button" icon="fa-folder-open">
                            <field name="task_count" widget="statinfo" string="Matters"/>
                        </button>
                    </div>
                    <div class="alert alert-danger mb-3" role="alert" invisible="not cap_exceeded">
                        <p class="mb-2">
                            <b>Above the 20% cap.</b>
                            The fee counted against the cap is <field name="cap_fee" class="d-inline fw-bold"/>,
                            but 20% of the value of the matters (<field name="cap_base" class="d-inline"/>) is
                            <field name="cap_limit" class="d-inline fw-bold"/>.
                            Outside criminal matters, Advocacy Law Art. 56 does not allow more. Only a legal manager can activate it, with a reason.
                        </p>
                        <field name="cap_override_reason" nolabel="1" invisible="not ldm_is_manager"
                               placeholder="Why the fee may exceed 20% of the value"/>
                    </div>
                    <div class="alert alert-warning mb-3" role="status" invisible="not retainer_below_minimum">
                        Below the Bar's minimum retainer of <field name="retainer_minimum" class="d-inline fw-bold"/> a month
                        for this kind of company (Iraqi Bar order 3021 of 2021; check the current amount).
                    </div>
                    <div class="oe_title">
                        <div class="text-muted"><field name="name" readonly="1"/></div>
                        <h1><field name="legal_company_id" placeholder="Client" class="w-100" options="{'no_create': True}"/></h1>
                    </div>
                    <group>
                        <group name="fee">
                            <field name="fee_type"/>
                            <label for="amount" string="Monthly fee" invisible="fee_type != 'retainer'"/>
                            <label for="amount" string="Fee per matter" invisible="fee_type != 'per_transaction'"/>
                            <label for="amount" string="Fee per session" invisible="fee_type != 'per_hearing'"/>
                            <label for="amount" invisible="fee_type in ('retainer', 'per_transaction', 'per_hearing')"/>
                            <div class="d-flex gap-2">
                                <field name="amount" class="oe_inline"/>
                                <field name="currency_id" class="oe_inline" options="{'no_create': True}"
                                       groups="base.group_multi_currency"/>
                            </div>
                            <field name="success_percent"/>
                            <field name="hourly_rate" invisible="fee_type != 'hourly'"/>
                            <field name="retainer_period" invisible="fee_type != 'retainer'"/>
                            <field name="bar_withholding" invisible="fee_type != 'retainer'"/>
                        </group>
                        <group name="terms">
                            <field name="lawyer_id" widget="many2one_avatar_user"/>
                            <field name="date_start"/>
                            <field name="date_end"/>
                            <field name="signed"/>
                            <field name="signed_date" invisible="not signed"/>
                            <field name="signed_copy" filename="signed_copy_name"/>
                            <field name="signed_copy_name" invisible="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="schedule" string="Schedule">
                            <field name="line_ids" context="{'default_engagement_id': id}">
                                <list editable="bottom" decoration-muted="state in ('paid', 'waived')"
                                      decoration-danger="is_overdue">
                                    <field name="sequence" widget="handle"/>
                                    <field name="name"/>
                                    <field name="trigger_event"/>
                                    <field name="date" optional="show"/>
                                    <field name="task_id" optional="hide" options="{'no_create': True}"
                                           domain="[('engagement_id', '=', parent.id)]"/>
                                    <field name="amount" sum="Total"/>
                                    <field name="currency_id" column_invisible="1"/>
                                    <field name="is_overdue" column_invisible="1"/>
                                    <field name="state" widget="ldm_pill" readonly="1"
                                           decoration-info="state == 'planned'" decoration-warning="state == 'due'"
                                           decoration-success="state in ('invoiced', 'paid')"/>
                                    <button name="action_ldm_mark_due" type="object" string="Due now" class="btn-link"
                                            invisible="state != 'planned'"
                                            groups="legal_department_management.group_legal_user,legal_department_management.group_ldm_billing_user"/>
                                    <button name="action_ldm_waive" type="object" string="Waive" class="btn-link"
                                            invisible="state not in ('planned', 'due')"
                                            groups="legal_department_management.group_legal_user,legal_department_management.group_ldm_billing_user"/>
                                    <button name="action_ldm_remind" type="object" icon="fa-whatsapp" title="Remind the client"
                                            invisible="state != 'due'" column_invisible="not parent.ldm_can_message"/>
                                </list>
                            </field>
                            <group class="oe_subtotal_footer">
                                <field name="amount_invoiced"/>
                                <field name="amount_due"/>
                                <field name="amount_planned"/>
                                <field name="next_line_text" invisible="not next_line_text"/>
                            </group>
                        </page>
                        <page name="matters" string="Matters">
                            <field name="task_ids" readonly="1">
                                <list>
                                    <field name="task_number"/>
                                    <field name="name"/>
                                    <field name="law_branch"/>
                                    <field name="matter_value"/>
                                    <field name="currency_id" column_invisible="1"/>
                                    <field name="state" widget="ldm_pill"/>
                                </list>
                            </field>
                            <p class="text-muted" invisible="task_ids">
                                Link matters from each matter's Money tab. The 20% cap is checked against their value.
                            </p>
                            <group invisible="not cap_applies">
                                <field name="cap_base"/>
                                <field name="cap_limit"/>
                                <field name="cap_fee"/>
                            </group>
                        </page>
                        <page name="terms" string="Scope and terms">
                            <field name="note" placeholder="What the office agreed to do, and what it did not."/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_engagement_search" model="ir.ui.view">
        <field name="name">legal.engagement.search</field>
        <field name="model">legal.engagement</field>
        <field name="arch" type="xml">
            <search string="Fee agreements">
                <field name="name"/>
                <field name="legal_company_id"/>
                <field name="lawyer_id"/>
                <filter string="Active" name="filter_active" domain="[('state', '=', 'active')]"/>
                <filter string="Draft" name="filter_draft" domain="[('state', '=', 'draft')]"/>
                <separator/>
                <filter string="Not signed" name="filter_unsigned" domain="[('signed', '=', False), ('state', '!=', 'closed')]"/>
                <filter string="Retainers" name="filter_retainer" domain="[('fee_type', '=', 'retainer')]"/>
                <group>
                    <filter string="Client" name="group_client" context="{'group_by': 'legal_company_id'}"/>
                    <filter string="Fee arrangement" name="group_type" context="{'group_by': 'fee_type'}"/>
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_engagement" model="ir.actions.act_window">
        <field name="name">Fee agreements</field>
        <field name="res_model">legal.engagement</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_ldm_engagement_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No fee agreements yet</p>
            <p>Record what each client pays and when: a lump sum, instalments tied to the case's stages, a success fee or a monthly retainer.</p>
        </field>
    </record>

    <!-- ================================================================
         To invoice
         ================================================================ -->
    <record id="view_ldm_billable_list" model="ir.ui.view">
        <field name="name">legal.billable.list</field>
        <field name="model">legal.billable</field>
        <field name="arch" type="xml">
            <list string="To invoice" create="0" edit="0" delete="0" decoration-danger="is_overdue" expand="1" class="o_ldm_view">
                <header>
                    <button name="action_ldm_invoice" type="object" string="Create invoice" class="btn-primary"
                            groups="account.group_account_invoice"/>
                </header>
                <field name="date"/>
                <field name="kind" widget="ldm_pill" decoration-info="kind == 'fee'" decoration-primary="kind == 'time'"/>
                <field name="name"/>
                <field name="task_id" optional="show"/>
                <field name="amount" sum="Total"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="is_overdue" column_invisible="1"/>
                <field name="ldm_can_message" column_invisible="1"/>
                <field name="quantity" optional="hide"/>
                <field name="engagement_id" optional="hide"/>
                <field name="user_id" optional="hide" widget="many2one_avatar_user"/>
                <button name="action_ldm_remind" type="object" icon="fa-whatsapp" title="Remind the client"
                        invisible="not ldm_can_message"/>
                <button name="action_ldm_open_source" type="object" icon="fa-external-link" title="Open"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_billable_search" model="ir.ui.view">
        <field name="name">legal.billable.search</field>
        <field name="model">legal.billable</field>
        <field name="arch" type="xml">
            <search string="To invoice">
                <field name="legal_company_id"/>
                <field name="task_id"/>
                <field name="engagement_id"/>
                <filter string="Fees" name="filter_fee" domain="[('kind', '=', 'fee')]"/>
                <filter string="Time" name="filter_time" domain="[('kind', '=', 'time')]"/>
                <filter string="Expenses" name="filter_expense" domain="[('kind', '=', 'expense')]"/>
                <group>
                    <filter string="Client and currency" name="group_invoice" context="{'group_by': 'group_key'}"/>
                    <filter string="Client" name="group_client" context="{'group_by': 'legal_company_id'}"/>
                    <filter string="Currency" name="group_currency" context="{'group_by': 'currency_id'}"/>
                    <filter string="Matter" name="group_task" context="{'group_by': 'task_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_to_invoice" model="ir.actions.act_window">
        <field name="name">To invoice</field>
        <field name="res_model">legal.billable</field>
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_ldm_billable_list"/>
        <field name="domain">[]</field>
        <field name="search_view_id" ref="view_ldm_billable_search"/>
        <field name="context">{'search_default_group_invoice': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Nothing to invoice</p>
            <p>Instalments appear here when they fall due, time once it is approved, and expenses recharged to the client once confirmed.</p>
        </field>
    </record>

    <!-- ================================================================
         Time
         ================================================================ -->
    <record id="view_ldm_time_list" model="ir.ui.view">
        <field name="name">legal.time.entry.list</field>
        <field name="model">legal.time.entry</field>
        <field name="arch" type="xml">
            <list string="Time" editable="top" sample="1" decoration-muted="state == 'invoiced'" class="o_ldm_view">
                <header>
                    <button name="action_ldm_approve" type="object" string="Approve"
                            groups="legal_department_management.group_legal_user,legal_department_management.group_ldm_billing_user"/>
                </header>
                <field name="date"/>
                <field name="task_id" options="{'no_create': True}" readonly="state != 'draft'"/>
                <field name="description" readonly="state == 'invoiced'"/>
                <field name="duration" widget="float_time" sum="Total" readonly="state != 'draft'"/>
                <field name="billable" optional="show" readonly="state != 'draft'"/>
                <field name="state" widget="ldm_pill" readonly="1" decoration-info="state == 'draft'"
                       decoration-success="state == 'approved'"/>
                <field name="user_id" optional="hide" widget="many2one_avatar_user"/>
                <field name="rate" optional="hide" readonly="state != 'draft'"
                       groups="legal_department_management.group_ldm_billing"/>
                <field name="amount" optional="hide" sum="Total" groups="legal_department_management.group_ldm_billing"/>
                <field name="currency_id" column_invisible="1"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_time_form" model="ir.ui.view">
        <field name="name">legal.time.entry.form</field>
        <field name="model">legal.time.entry</field>
        <field name="arch" type="xml">
            <form string="Time entry" class="o_ldm_view">
                <field name="ldm_can_approve" invisible="1"/>
                <header>
                    <button name="action_ldm_approve" type="object" string="Approve" class="btn-primary"
                            invisible="state != 'draft' or not ldm_can_approve"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="task_id" options="{'no_create': True}"/>
                            <field name="description"/>
                            <field name="user_id" widget="many2one_avatar_user"/>
                        </group>
                        <group>
                            <field name="date"/>
                            <field name="duration" widget="float_time"/>
                            <field name="billable"/>
                            <field name="rate" groups="legal_department_management.group_ldm_billing"/>
                            <field name="amount" groups="legal_department_management.group_ldm_billing"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_ldm_time_search" model="ir.ui.view">
        <field name="name">legal.time.entry.search</field>
        <field name="model">legal.time.entry</field>
        <field name="arch" type="xml">
            <search string="Time">
                <field name="description"/>
                <field name="task_id"/>
                <field name="user_id"/>
                <filter string="Mine" name="filter_mine" domain="[('user_id', '=', uid)]"/>
                <separator/>
                <filter string="Today" name="filter_today" domain="[('date', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter string="Date" name="filter_date" date="date"/>
                <separator/>
                <filter string="To approve" name="filter_draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Billable" name="filter_billable" domain="[('billable', '=', True)]"/>
                <group>
                    <filter string="Day" name="group_day" context="{'group_by': 'date:day'}"/>
                    <filter string="Matter" name="group_task" context="{'group_by': 'task_id'}"/>
                    <filter string="Person" name="group_user" context="{'group_by': 'user_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="view_ldm_time_pivot" model="ir.ui.view">
        <field name="name">legal.time.entry.pivot</field>
        <field name="model">legal.time.entry</field>
        <field name="arch" type="xml">
            <pivot string="Time analysis" sample="1" class="o_ldm_view">
                <field name="user_id" type="row"/>
                <field name="date" interval="week" type="col"/>
                <field name="duration" type="measure" widget="float_time"/>
            </pivot>
        </field>
    </record>

    <record id="action_ldm_time_entry" model="ir.actions.act_window">
        <field name="name">Time</field>
        <field name="res_model">legal.time.entry</field>
        <field name="view_mode">list,form,pivot</field>
        <field name="view_id" ref="view_ldm_time_list"/>
        <field name="search_view_id" ref="view_ldm_time_search"/>
        <field name="context">{'search_default_filter_mine': 1, 'search_default_filter_today': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No time recorded</p>
            <p>Add a line for each piece of work: the matter, what was done and how long (1.5 or 1:30), or use the timer on the top bar.</p>
        </field>
    </record>

    <!-- ================================================================
         Conflict checks
         ================================================================ -->
    <record id="view_ldm_conflict_check_list" model="ir.ui.view">
        <field name="name">legal.conflict.check.list</field>
        <field name="model">legal.conflict.check</field>
        <field name="arch" type="xml">
            <list string="Conflict checks" create="0" edit="0" delete="0" sample="1"
                  decoration-danger="decision == 'pending'" decoration-muted="decision == 'superseded'" class="o_ldm_view">
                <field name="date"/>
                <field name="query"/>
                <field name="task_id"/>
                <field name="hit_count" string="Conflicts"/>
                <field name="decision" widget="ldm_pill" decoration-warning="decision == 'pending'"
                       decoration-success="decision == 'clear'" decoration-info="decision == 'override'"
                       decoration-danger="decision == 'declined'"/>
                <field name="user_id" widget="many2one_avatar_user" optional="show"/>
                <field name="decided_by_id" optional="hide"/>
                <field name="seen_count" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_conflict_check_form" model="ir.ui.view">
        <field name="name">legal.conflict.check.form</field>
        <field name="model">legal.conflict.check</field>
        <field name="arch" type="xml">
            <form string="Conflict check" create="0" edit="0" delete="0" class="o_ldm_view">
                <field name="ldm_is_manager" invisible="1"/>
                <header>
                    <button name="action_ldm_decide" type="object" string="Decide" class="btn-primary"
                            invisible="decision != 'pending' or not ldm_is_manager"/>
                    <button name="action_ldm_send_to_partner" type="object" string="Ask a partner to decide"
                            invisible="decision != 'pending' or ldm_is_manager"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <field name="decision" widget="statusbar" statusbar_visible="pending"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <div class="text-muted">Names checked</div>
                        <h2><field name="query"/></h2>
                    </div>
                    <group>
                        <group>
                            <field name="task_id"/>
                            <field name="legal_company_id"/>
                            <field name="user_id"/>
                            <field name="date"/>
                        </group>
                        <group>
                            <field name="hit_count" string="Possible conflicts"/>
                            <field name="seen_count"/>
                            <field name="policy"/>
                            <field name="decided_by_id" invisible="not decided_by_id"/>
                            <field name="decided_on" invisible="not decided_on"/>
                        </group>
                    </group>
                    <separator string="What was found"/>
                    <field name="hit_html" nolabel="1"/>
                    <group invisible="not reason">
                        <field name="reason"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_ldm_conflict_check_search" model="ir.ui.view">
        <field name="name">legal.conflict.check.search</field>
        <field name="model">legal.conflict.check</field>
        <field name="arch" type="xml">
            <search string="Conflict checks">
                <field name="query"/>
                <field name="task_id"/>
                <filter string="Waiting for a decision" name="filter_pending" domain="[('decision', '=', 'pending')]"/>
                <filter string="With possible conflicts" name="filter_hits" domain="[('hit_count', '&gt;', 0)]"/>
                <separator/>
                <filter string="Date" name="filter_date" date="date"/>
                <group>
                    <filter string="Decision" name="group_decision" context="{'group_by': 'decision'}"/>
                    <filter string="Checked by" name="group_user" context="{'group_by': 'user_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_conflict_check" model="ir.actions.act_window">
        <field name="name">Conflict checks</field>
        <field name="res_model">legal.conflict.check</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ldm_conflict_check_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No conflict checks yet</p>
            <p>A check runs whenever a matter is opened against someone. Every check is kept here, with its decision.</p>
        </field>
    </record>

    <!-- ================================================================
         Matters without a signed fee agreement (Advocacy Law Arts. 59, 65)
         ================================================================ -->
    <record id="action_ldm_task_no_agreement" model="ir.actions.act_window">
        <field name="name">Without a signed fee agreement</field>
        <field name="res_model">legal.task</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('ldm_no_signed_agreement', '=', True)]</field>
        <field name="context">{'search_default_filter_open': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Every open matter has a signed fee agreement</p>
            <p>Without a written agreement the fee is the customary rate and a claim lapses after three years (Advocacy Law Arts. 59 and 65).</p>
        </field>
    </record>

    <!-- ================================================================
         Wizards
         ================================================================ -->
    <record id="view_ldm_invoice_wizard_form" model="ir.ui.view">
        <field name="name">legal.invoice.wizard.form</field>
        <field name="model">legal.invoice.wizard</field>
        <field name="arch" type="xml">
            <form string="Create invoices" class="o_ldm_view">
                <field name="invoice_count" invisible="1"/>
                <div class="mb-2">
                    <field name="summary" nolabel="1"/>
                </div>
                <p class="text-muted small mb-2">The invoices are created as drafts: check the date and the lines on each before sending it.</p>
                <group groups="account.group_account_manager">
                    <field name="journal_id" options="{'no_create': True}"/>
                </group>
                <field name="billable_ids" nolabel="1" options="{'link': False, 'create': False}">
                    <list create="0" edit="0">
                        <field name="legal_company_id"/>
                        <field name="kind" widget="ldm_pill"/>
                        <field name="name"/>
                        <field name="task_id" optional="show"/>
                        <field name="amount" sum="Total"/>
                        <field name="currency_id" column_invisible="1"/>
                    </list>
                </field>
                <footer>
                    <button name="action_create" type="object" string="Create invoices" class="btn-primary"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_statement_wizard_form" model="ir.ui.view">
        <field name="name">legal.statement.wizard.form</field>
        <field name="model">legal.statement.wizard</field>
        <field name="arch" type="xml">
            <form string="Statement of account" class="o_ldm_view">
                <group>
                    <field name="legal_company_id" options="{'no_create': True}"/>
                    <label for="date_from" string="Period"/>
                    <div class="d-flex gap-2 align-items-center">
                        <field name="date_from" class="oe_inline"/>
                        <span>to</span>
                        <field name="date_to" class="oe_inline"/>
                    </div>
                </group>
                <footer>
                    <button name="action_print" type="object" string="Print" class="btn-primary"/>
                    <button name="action_message" type="object" string="Tell the client"
                            groups="legal_department_management.group_ldm_client_messages"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_fund_move_wizard_form" model="ir.ui.view">
        <field name="name">legal.fund.move.wizard.form</field>
        <field name="model">legal.fund.move.wizard</field>
        <field name="arch" type="xml">
            <form string="Client money" class="o_ldm_view">
                <field name="goes_negative" invisible="1"/>
                <field name="is_manager" invisible="1"/>
                <group>
                    <group>
                        <field name="kind"/>
                        <field name="legal_company_id" options="{'no_create': True}"/>
                        <field name="task_id" options="{'no_create': True}"/>
                        <label for="amount"/>
                        <div class="d-flex gap-2">
                            <field name="amount" class="oe_inline"/>
                            <field name="currency_id" class="oe_inline" options="{'no_create': True}"
                                   groups="base.group_multi_currency"/>
                        </div>
                    </group>
                    <group>
                        <field name="date"/>
                        <field name="receipt_number"/>
                        <field name="receipt_file" filename="receipt_filename"/>
                        <field name="receipt_filename" invisible="1"/>
                        <field name="note"/>
                    </group>
                </group>
                <div class="alert alert-info mb-2" role="status" invisible="not legal_company_id">
                    Held for this client now: <field name="balance" class="d-inline fw-bold"/>;
                    afterwards: <field name="balance_after" class="d-inline fw-bold"/>.
                </div>
                <div class="alert alert-danger" role="alert" invisible="not goes_negative">
                    <p class="mb-2">This would take the client's money below zero.</p>
                    <p class="mb-0" invisible="is_manager">Only a legal manager can allow it.</p>
                    <field name="override_reason" invisible="not is_manager" placeholder="Why it may go below zero"/>
                </div>
                <footer>
                    <button name="action_confirm" type="object" string="Save" class="btn-primary"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_advance_settle_wizard_form" model="ir.ui.view">
        <field name="name">legal.advance.settle.wizard.form</field>
        <field name="model">legal.advance.settle.wizard</field>
        <field name="arch" type="xml">
            <form string="Settle a cash advance" class="o_ldm_view">
                <field name="currency_id" invisible="1"/>
                <field name="candidate_ids" invisible="1"/>
                <group>
                    <group>
                        <field name="advance_id" readonly="1"/>
                        <field name="user_id" widget="many2one_avatar_user"/>
                        <field name="amount"/>
                    </group>
                    <group>
                        <field name="spent"/>
                        <field name="returned_amount"/>
                        <field name="balance" class="fw-bold" decoration-danger="balance &gt; 0"/>
                    </group>
                </group>
                <p class="text-muted">The receipts paid from this advance. Remove any that do not belong to it.</p>
                <field name="expense_ids" nolabel="1" domain="[('id', 'in', candidate_ids)]">
                    <list create="0" edit="0">
                        <field name="date"/>
                        <field name="task_id"/>
                        <field name="name"/>
                        <field name="receipt_number"/>
                        <field name="amount" sum="Total"/>
                        <field name="currency_id" column_invisible="1"/>
                    </list>
                </field>
                <footer>
                    <button name="action_confirm" type="object" string="Settle" class="btn-primary"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_conflict_decision_wizard_form" model="ir.ui.view">
        <field name="name">legal.conflict.decision.wizard.form</field>
        <field name="model">legal.conflict.decision.wizard</field>
        <field name="arch" type="xml">
            <form string="Decide on the conflict check" class="o_ldm_view">
                <field name="check_id" invisible="1"/>
                <group>
                    <field name="task_id" readonly="1" invisible="not task_id"/>
                    <field name="query" readonly="1"/>
                    <field name="policy" readonly="1"/>
                </group>
                <field name="hit_html" nolabel="1" readonly="1"/>
                <group>
                    <field name="decision" widget="radio"/>
                    <field name="reason" placeholder="Kept with the check as evidence of the decision."/>
                </group>
                <footer>
                    <button name="action_confirm" type="object" string="Record the decision" class="btn-primary"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_client_message_wizard_form" model="ir.ui.view">
        <field name="name">legal.client.message.wizard.form</field>
        <field name="model">legal.client.message.wizard</field>
        <field name="arch" type="xml">
            <form string="Message the client" class="o_ldm_view">
                <field name="phone_ok" invisible="1"/>
                <field name="task_id" invisible="1"/>
                <field name="legal_company_id" invisible="1"/>
                <field name="hearing_id" invisible="1"/>
                <field name="fee_line_id" invisible="1"/>
                <group>
                    <group>
                        <field name="template_id" options="{'no_create': True, 'no_open': True}"/>
                        <field name="partner_id" readonly="1"/>
                    </group>
                    <group>
                        <field name="phone" placeholder="07XX XXX XXXX" class="o_ldm_ltr"/>
                        <field name="lang"/>
                    </group>
                </group>
                <field name="body" nolabel="1" placeholder="The message the client will receive."/>
                <footer>
                    <button name="action_whatsapp" type="object" string="Open in WhatsApp" class="btn-primary"
                            icon="fa-whatsapp"/>
                    <button name="action_email" type="object" string="Send by email" invisible="not email"/>
                    <field name="email" invisible="1"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_expense_post_wizard_form" model="ir.ui.view">
        <field name="name">legal.expense.post.wizard.form</field>
        <field name="model">legal.expense.post.wizard</field>
        <field name="arch" type="xml">
            <form string="Post to accounting" class="o_ldm_view">
                <group>
                    <group>
                        <field name="mode" widget="radio"/>
                        <field name="journal_id" options="{'no_create': True}"/>
                    </group>
                    <group>
                        <field name="date"/>
                        <field name="expense_account_id" options="{'no_create': True}"/>
                    </group>
                </group>
                <field name="expense_ids" nolabel="1" readonly="1">
                    <list>
                        <field name="task_id"/>
                        <field name="category_id"/>
                        <field name="name"/>
                        <field name="amount" sum="Total"/>
                        <field name="currency_id" column_invisible="1"/>
                    </list>
                </field>
                <footer>
                    <button name="action_post" type="object" string="Post" class="btn-primary"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_fee_waive_wizard_form" model="ir.ui.view">
        <field name="name">legal.fee.waive.wizard.form</field>
        <field name="model">legal.fee.waive.wizard</field>
        <field name="arch" type="xml">
            <form string="Waive instalments" class="o_ldm_view">
                <field name="line_ids" nolabel="1" readonly="1">
                    <list>
                        <field name="name"/>
                        <field name="amount"/>
                        <field name="currency_id" column_invisible="1"/>
                    </list>
                </field>
                <group>
                    <field name="reason" placeholder="The reason is kept on the agreement."/>
                </group>
                <footer>
                    <button name="action_confirm" type="object" string="Waive" class="btn-primary"/>
                    <button string="Discard" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- The close dialog in billing mode (foundation dialog, extended) -->
    <record id="view_ldm_task_decision_wizard_form_money" model="ir.ui.view">
        <field name="name">legal.task.decision.wizard.form.money</field>
        <field name="model">legal.task.decision.wizard</field>
        <field name="inherit_id" ref="view_ldm_task_decision_wizard_form"/>
        <field name="arch" type="xml">
            <xpath expr="//footer" position="before">
                <field name="ldm_billing" invisible="1"/>
                <div class="o_ldm_close_money border-top pt-3 mt-2" invisible="not ldm_billing">
                    <h5>Before closing</h5>
                    <group>
                        <group>
                            <field name="ldm_unbilled_text" invisible="not ldm_unbilled_text"/>
                            <field name="ldm_remaining_text" invisible="not ldm_remaining_text"/>
                            <field name="ldm_balance_text" invisible="not ldm_balance_text"/>
                        </group>
                        <group>
                            <field name="ldm_funds_text" invisible="not ldm_funds_text"/>
                            <field name="ldm_originals_count" invisible="not ldm_originals_count"/>
                            <field name="ldm_poa_id" invisible="not ldm_poa_id"/>
                        </group>
                    </group>
                    <p class="text-muted small" invisible="not ldm_funds_text and not ldm_originals_count">
                        Return the client's money and original documents when the matter ends (Advocacy Law Art. 53).
                    </p>
                    <group invisible="not ldm_remaining_text">
                        <field name="ldm_remaining_action" widget="radio"/>
                        <field name="ldm_waive_reason" invisible="ldm_remaining_action != 'waive'"
                               placeholder="Why the remaining fee is waived"/>
                    </group>
                </div>
            </xpath>
        </field>
    </record>

    <!-- ================================================================
         The matter cockpit (foundation form, extended)
         ================================================================ -->
    <record id="view_legal_task_form_money" model="ir.ui.view">
        <field name="name">legal.task.form.money</field>
        <field name="model">legal.task</field>
        <field name="inherit_id" ref="view_legal_task_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='ldm_is_manager']" position="after">
                <field name="ldm_conflict_state" invisible="1"/>
                <field name="ldm_no_signed_agreement" invisible="1"/>
                <field name="ldm_can_message" invisible="1"/>
                <field name="ldm_fund_balance_text" invisible="1"/>
                <field name="ldm_invoice_count" invisible="1"/>
                <field name="ldm_time_hours" invisible="1"/>
            </xpath>
            <!-- Starting is refused while a conflict check waits for a partner: do not offer it. -->
            <xpath expr="(//header/button[@name='action_set_in_progress'])[1]" position="attributes">
                <attribute name="invisible">state != 'draft' or ldm_can_request_approval or approval_state == 'to_approve' or ldm_conflict_state in ('pending', 'declined')</attribute>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_ldm_client_money" type="object" class="oe_stat_button" icon="fa-university"
                        invisible="not ldm_fund_balance_text" groups="legal_department_management.group_ldm_billing">
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_value"><field name="ldm_fund_balance_text" readonly="1"/></span>
                        <span class="o_stat_text">Client money</span>
                    </div>
                </button>
                <button name="action_ldm_invoices" type="object" class="oe_stat_button" icon="fa-file-text-o"
                        invisible="not ldm_invoice_count"
                        groups="account.group_account_invoice,account.group_account_readonly">
                    <field name="ldm_invoice_count" widget="statinfo" string="Invoices"/>
                </button>
                <button name="action_ldm_time" type="object" class="oe_stat_button" icon="fa-clock-o"
                        invisible="not ldm_time_hours" groups="legal_department_management.group_ldm_time">
                    <field name="ldm_time_hours" widget="statinfo" string="Hours"/>
                </button>
                <button name="action_ldm_message" type="object" class="oe_stat_button" icon="fa-whatsapp"
                        invisible="not ldm_can_message">
                    <div class="o_stat_info"><span class="o_stat_text">Message the client</span></div>
                </button>
            </xpath>
            <xpath expr="//div[hasclass('oe_title')]" position="before">
                <div class="alert alert-danger d-flex flex-wrap align-items-center gap-2 mb-3" role="alert"
                     invisible="ldm_conflict_state != 'pending'">
                    <i class="fa fa-exclamation-triangle" title="Possible conflict of interest" role="img"/>
                    <span class="flex-grow-1">Possible conflict of interest. A legal manager must decide before work starts.</span>
                    <button name="action_ldm_open_conflict" type="object" string="Decide" class="btn btn-sm btn-primary"
                            invisible="not ldm_is_manager" groups="legal_department_management.group_legal_manager"/>
                    <button name="action_ldm_open_conflict" type="object" string="See the matches" class="btn btn-sm btn-secondary"
                            invisible="ldm_is_manager" groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_ask_conflict_decision" type="object" string="Ask a partner to decide"
                            class="btn btn-sm btn-link" invisible="ldm_is_manager"
                            groups="legal_department_management.group_ldm_clerk"/>
                </div>
                <div class="alert alert-secondary mb-3" role="status" invisible="ldm_conflict_state != 'declined'">
                    Declined after the conflict check. This matter cannot start.
                </div>
            </xpath>
            <xpath expr="//div[hasclass('oe_title')]/div" position="inside">
                <span class="badge rounded-pill text-bg-warning align-self-center" invisible="not ldm_no_signed_agreement"
                      groups="legal_department_management.group_ldm_billing">No signed fee agreement</span>
            </xpath>
            <xpath expr="//page[@name='money']" position="attributes">
                <attribute name="string">Money</attribute>
            </xpath>
            <xpath expr="//page[@name='money']/field[@name='expense_ids']" position="before">
                <group name="money_fees" groups="legal_department_management.group_ldm_billing">
                    <group>
                        <field name="engagement_id" options="{'no_create': True}"
                               domain="[('legal_company_id', '=', legal_company_id), ('state', '!=', 'closed')]"/>
                        <field name="billing_state" widget="ldm_pill" decoration-warning="billing_state == 'to_invoice'"
                               decoration-success="billing_state == 'invoiced'"/>
                    </group>
                    <group>
                        <field name="ldm_unbilled_text" invisible="not ldm_unbilled_text"/>
                        <field name="ldm_fund_balance_text" string="Client money held" invisible="not ldm_fund_balance_text"/>
                    </group>
                </group>
            </xpath>
            <xpath expr="//page[@name='money']/field[@name='expense_ids']/list/field[@name='receipt_number']" position="attributes">
                <attribute name="optional">hide</attribute>
            </xpath>
            <xpath expr="//page[@name='money']/field[@name='expense_ids']/list/field[@name='recoverable']" position="attributes">
                <attribute name="optional">show</attribute>
                <attribute name="string">Recharge</attribute>
            </xpath>
            <xpath expr="//page[@name='money']/field[@name='expense_ids']" position="inside">
                <kanban class="o_ldm_kanban">
                    <field name="name"/>
                    <field name="date"/>
                    <field name="category_id"/>
                    <field name="amount"/>
                    <field name="currency_id"/>
                    <field name="paid_by"/>
                    <field name="receipt_number"/>
                    <templates>
                        <t t-name="card">
                            <div class="d-flex justify-content-between gap-2">
                                <field name="name" class="fw-bold text-truncate"/>
                                <field name="amount" widget="monetary" class="text-nowrap"/>
                            </div>
                            <div class="text-muted small text-truncate">
                                <field name="category_id"/> · <field name="date"/>
                            </div>
                            <div class="small text-truncate">
                                <field name="paid_by"/>
                                <t t-if="record.receipt_number.raw_value"> · <field name="receipt_number"/></t>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </xpath>
            <xpath expr="//page[@name='money']/field[@name='expense_ids']/list/field[@name='amount']" position="after">
                <field name="paid_by" optional="show"/>
                <field name="employee_user_id" optional="hide" widget="many2one_avatar_user"
                       readonly="paid_by != 'employee'"/>
                <field name="billed" optional="hide" groups="legal_department_management.group_ldm_billing"/>
            </xpath>
            <xpath expr="//page[@name='money']/group[hasclass('oe_subtotal_footer')]" position="after">
                <div groups="legal_department_management.group_ldm_time">
                    <separator string="Time"/>
                    <field name="time_entry_ids" context="{'default_task_id': id}">
                        <kanban class="o_ldm_kanban">
                            <field name="description"/>
                            <field name="duration"/>
                            <field name="date"/>
                            <field name="user_id"/>
                            <field name="state"/>
                            <templates>
                                <t t-name="card">
                                    <div class="d-flex justify-content-between gap-2">
                                        <field name="description" class="text-truncate"/>
                                        <field name="duration" widget="float_time" class="fw-bold text-nowrap"/>
                                    </div>
                                    <div class="text-muted small d-flex gap-2">
                                        <field name="date"/>
                                        <field name="user_id" widget="many2one_avatar_user"/>
                                        <field name="state" class="ms-auto"/>
                                    </div>
                                </t>
                            </templates>
                        </kanban>
                        <list editable="bottom" decoration-muted="state == 'invoiced'">
                            <field name="date"/>
                            <field name="user_id" widget="many2one_avatar_user"/>
                            <field name="description"/>
                            <field name="duration" widget="float_time" sum="Total"/>
                            <field name="billable" optional="show"/>
                            <field name="state" widget="ldm_pill" readonly="1"/>
                        </list>
                    </field>
                </div>
            </xpath>
            <xpath expr="//field[@name='hearing_ids']/list" position="inside">
                <button name="action_ldm_client_message" type="object" icon="fa-whatsapp" title="Message the client"
                        column_invisible="not parent.ldm_can_message"/>
            </xpath>
        </field>
    </record>

    <record id="view_legal_task_search_money" model="ir.ui.view">
        <field name="name">legal.task.search.money</field>
        <field name="model">legal.task</field>
        <field name="inherit_id" ref="view_legal_task_search"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='filter_confidential']" position="after">
                <filter string="No signed fee agreement" name="filter_no_agreement"
                        domain="[('ldm_no_signed_agreement', '=', True)]"
                        groups="legal_department_management.group_ldm_billing"/>
                <filter string="Conflict to decide" name="filter_conflict_pending"
                        domain="[('ldm_conflict_state', '=', 'pending')]"
                        groups="legal_department_management.group_ldm_conflicts"/>
            </xpath>
        </field>
    </record>

    <!-- ================================================================
         The client dossier (foundation form, extended)
         ================================================================ -->
    <record id="view_legal_company_form_money" model="ir.ui.view">
        <field name="name">legal.company.form.money</field>
        <field name="model">legal.company</field>
        <field name="inherit_id" ref="view_legal_company_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='currency_id']" position="after">
                <field name="ldm_can_message" invisible="1"/>
                <field name="ldm_fund_balance_text" invisible="1"/>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_ldm_client_money" type="object" class="oe_stat_button" icon="fa-university"
                        groups="legal_department_management.group_ldm_billing">
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_value">
                            <field name="ldm_fund_balance_text" readonly="1" invisible="not ldm_fund_balance_text"/>
                            <span invisible="ldm_fund_balance_text">0</span>
                        </span>
                        <span class="o_stat_text">Client money</span>
                    </div>
                </button>
                <button name="action_ldm_to_invoice" type="object" class="oe_stat_button" icon="fa-list-alt"
                        invisible="not unbilled_amount"
                        groups="legal_department_management.group_ldm_billing_user,legal_department_management.group_legal_manager">
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_value"><field name="unbilled_amount" widget="monetary"/></span>
                        <span class="o_stat_text">To invoice</span>
                    </div>
                </button>
                <button name="action_ldm_open_invoices" type="object" class="oe_stat_button" icon="fa-file-text-o"
                        groups="account.group_account_invoice,account.group_account_readonly">
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_value"><field name="balance_due" widget="monetary"/></span>
                        <span class="o_stat_text">Balance due</span>
                    </div>
                </button>
                <button name="action_ldm_engagements" type="object" class="oe_stat_button" icon="fa-handshake-o"
                        groups="legal_department_management.group_ldm_billing">
                    <field name="engagement_count" widget="statinfo" string="Fee agreements"/>
                </button>
                <button name="action_ldm_message" type="object" class="oe_stat_button" icon="fa-whatsapp"
                        invisible="not ldm_can_message">
                    <div class="o_stat_info"><span class="o_stat_text">Message the client</span></div>
                </button>
            </xpath>
            <xpath expr="//page[@name='matters']" position="after">
                <page name="engagements" string="Fee agreements" groups="legal_department_management.group_ldm_billing">
                    <field name="engagement_ids" readonly="1">
                        <list>
                            <field name="name"/>
                            <field name="fee_type"/>
                            <field name="amount"/>
                            <field name="currency_id" column_invisible="1"/>
                            <field name="signed"/>
                            <field name="state" widget="ldm_pill"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <!-- ================================================================
         Settings
         ================================================================ -->
    <record id="res_config_settings_view_form_money" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.ldm.money</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//setting[@id='ldm_accounting_setting']" position="after">
                <setting id="ldm_iqd_setting" string="Iraqi dinar without fils" invisible="not ldm_iqd_can_round"
                         help="Show and round IQD amounts in whole dinars. Possible only before any accounting entry in dinars is posted.">
                    <field name="ldm_iqd_can_round" invisible="1"/>
                    <button name="action_ldm_iqd_whole_dinars" type="object" string="Show IQD in whole dinars"
                            class="btn-secondary" confirm="IQD amounts will be rounded to whole dinars from now on. Continue?"/>
                </setting>
            </xpath>
        </field>
    </record>

    <!-- ================================================================
         Less frequent actions: the Actions (gear) menu
         ================================================================ -->
    <record id="action_server_ldm_task_invoice" model="ir.actions.server">
        <field name="name">Create invoice</field>
        <field name="model_id" ref="model_legal_task"/>
        <field name="binding_model_id" ref="model_legal_task"/>
        <field name="binding_view_types">list,form</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_billing_user'))]"/>
        <field name="state">code</field>
        <field name="code">action = records.action_ldm_invoice()</field>
    </record>
    <record id="action_server_ldm_company_statement" model="ir.actions.server">
        <field name="name">Statement of account</field>
        <field name="model_id" ref="model_legal_company"/>
        <field name="binding_model_id" ref="model_legal_company"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_billing_user'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_statement()</field>
    </record>
    <record id="action_server_ldm_company_payment" model="ir.actions.server">
        <field name="name">Receive a payment</field>
        <field name="model_id" ref="model_legal_company"/>
        <field name="binding_model_id" ref="model_legal_company"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_billing_user'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_receive_payment()</field>
    </record>
    <record id="action_server_ldm_company_invoice" model="ir.actions.server">
        <field name="name">Create invoice</field>
        <field name="model_id" ref="model_legal_company"/>
        <field name="binding_model_id" ref="model_legal_company"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_billing_user'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_invoice()</field>
    </record>
    <record id="action_server_ldm_time_approve" model="ir.actions.server">
        <field name="name">Approve</field>
        <field name="model_id" ref="model_legal_time_entry"/>
        <field name="binding_model_id" ref="model_legal_time_entry"/>
        <field name="binding_view_types">list</field>
        <field name="group_ids" eval="[(4, ref('group_legal_user')), (4, ref('group_ldm_billing_user'))]"/>
        <field name="state">code</field>
        <field name="code">action = records.action_ldm_approve()</field>
    </record>

    <!-- ================================================================
         Menus (under the foundation's folders)
         ================================================================ -->
    <menuitem id="menu_ldm_expenses" name="Expenses" parent="menu_ldm_money" sequence="5"
              action="action_ldm_expense"
              groups="legal_department_management.group_ldm_clerk,legal_department_management.group_ldm_billing_user,legal_department_management.group_ldm_auditor"/>
    <menuitem id="menu_ldm_no_agreement" name="Without a signed agreement" parent="menu_ldm_money" sequence="6"
              action="action_ldm_task_no_agreement"
              groups="legal_department_management.group_legal_user,legal_department_management.group_ldm_billing_user,legal_department_management.group_ldm_auditor"/>
</odoo>
