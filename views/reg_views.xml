<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        Registers: powers of attorney, correspondence, letter templates,
        requests from other departments, legal opinions, letters of guarantee,
        and the register reports. The foundation's actions (same xmlids) are
        redefined here with these views; its menus already point at them.
    -->

    <!-- ============================================================
         Powers of attorney
         ============================================================ -->
    <record id="view_ldm_poa_list" model="ir.ui.view">
        <field name="name">legal.poa.list</field>
        <field name="model">legal.poa</field>
        <field name="arch" type="xml">
            <list string="Powers of attorney" sample="1"
                  decoration-muted="state in ('expired', 'revoked')">
                <field name="principal_company_id"/>
                <field name="poa_type"/>
                <field name="number"/>
                <field name="agent_user_ids" widget="many2many_avatar_user"/>
                <field name="date_expiry"/>
                <field name="expiry_status" widget="badge"
                       decoration-danger="expiry_status in ('expired', 'revoked')"
                       decoration-warning="expiry_status == 'expiring'"
                       decoration-success="expiry_status == 'valid'"/>
                <field name="state" column_invisible="1"/>
                <field name="agent_partner_ids" widget="many2many_tags" optional="hide"/>
                <field name="body_ids" widget="many2many_tags" optional="hide"/>
                <field name="notary_office" optional="hide"/>
                <field name="date_issued" optional="hide"/>
                <field name="open_task_count" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_poa_kanban" model="ir.ui.view">
        <field name="name">legal.poa.kanban</field>
        <field name="model">legal.poa</field>
        <field name="arch" type="xml">
            <kanban class="o_ldm_kanban" sample="1" quick_create="false">
                <field name="principal_company_id"/>
                <field name="poa_type"/>
                <field name="number"/>
                <field name="date_expiry"/>
                <field name="expiry_status"/>
                <field name="agent_user_ids"/>
                <templates>
                    <t t-name="card">
                        <field name="principal_company_id" class="fw-bold text-truncate"/>
                        <div class="text-muted small text-truncate">
                            <field name="poa_type"/>
                            <t t-if="record.number.raw_value"> · <field name="number"/></t>
                        </div>
                        <footer class="pt-1 align-items-center">
                            <field name="expiry_status" widget="badge"
                                   decoration-danger="expiry_status in ('expired', 'revoked')"
                                   decoration-warning="expiry_status == 'expiring'"
                                   decoration-success="expiry_status == 'valid'"/>
                            <field name="date_expiry" class="small ms-2" invisible="not date_expiry"/>
                            <field name="agent_user_ids" widget="many2many_avatar_user" class="ms-auto"/>
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_ldm_poa_form" model="ir.ui.view">
        <field name="name">legal.poa.form</field>
        <field name="model">legal.poa</field>
        <field name="arch" type="xml">
            <form string="Power of attorney">
                <field name="expiry_status" invisible="1"/>
                <header>
                    <button name="action_ldm_revoke" string="Revoke" type="object"
                            invisible="state == 'revoked' or not id"
                            groups="legal_department_management.group_legal_user"/>
                    <field name="state" widget="statusbar" statusbar_visible="active,expired,revoked"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_ldm_view_tasks" type="object" class="oe_stat_button" icon="fa-folder-open"
                                invisible="not task_count">
                            <field name="task_count" widget="statinfo" string="Matters"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Revoked" bg_color="text-bg-danger" invisible="state != 'revoked'"/>
                    <widget name="web_ribbon" title="Expired" bg_color="text-bg-warning" invisible="state != 'expired'"/>
                    <div class="alert alert-danger mb-3" role="alert" invisible="state != 'revoked'">
                        Revoked on <field name="revoked_date" class="d-inline" readonly="1"/>:
                        <field name="revoke_reason" class="d-inline" readonly="1"/>
                    </div>
                    <div class="alert alert-warning mb-3" role="status" invisible="expiry_status != 'expiring'">
                        Expires in <field name="days_to_expiry" class="d-inline"/> days. Once it is renewed,
                        enter the new expiry date.
                    </div>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1" placeholder="New power of attorney"/></h1>
                    </div>
                    <group name="essentials">
                        <group name="essentials_who">
                            <field name="principal_company_id" options="{'no_quick_create': True}"/>
                            <field name="poa_type"/>
                            <field name="number"/>
                            <field name="notary_office" invisible="poa_type == 'authorisation'"/>
                        </group>
                        <group name="essentials_when">
                            <field name="date_issued"/>
                            <field name="date_expiry"/>
                            <field name="agent_user_ids" widget="many2many_avatar_user"/>
                            <field name="agent_partner_ids" widget="many2many_tags"/>
                        </group>
                    </group>
                    <field name="scope" placeholder="The acts the agent may do, in the words of the deed."/>
                    <group name="more" string="More">
                        <group>
                            <field name="body_ids" widget="many2many_tags" placeholder="Empty: valid before every body"/>
                            <field name="substitution_allowed"/>
                            <field name="principal_partner_id"/>
                        </group>
                        <group>
                            <field name="original_location"/>
                            <field name="qr_reference"/>
                            <field name="scan_name" invisible="1"/>
                            <field name="scan" filename="scan_name"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_poa_search" model="ir.ui.view">
        <field name="name">legal.poa.search</field>
        <field name="model">legal.poa</field>
        <field name="arch" type="xml">
            <search string="Powers of attorney">
                <field name="name" string="Power of attorney"
                       filter_domain="['|', '|', ('name', 'ilike', self), ('number', 'ilike', self), ('notary_office', 'ilike', self)]"/>
                <field name="principal_company_id"/>
                <field name="body_ids" string="Valid before"
                       filter_domain="['|', ('body_ids', '=', False), ('body_ids', 'ilike', self)]"/>
                <field name="agent_user_ids" string="Agent"
                       filter_domain="['|', ('agent_user_ids', 'ilike', self), ('agent_partner_ids', 'ilike', self)]"/>
                <filter string="Can act today" name="filter_can_act"
                        domain="[('state', '=', 'active'), '|', ('date_expiry', '=', False), ('date_expiry', '&gt;=', context_today().strftime('%Y-%m-%d')), '|', ('date_issued', '=', False), ('date_issued', '&lt;=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter string="Expiring soon" name="filter_expiring" domain="[('expiry_status', '=', 'expiring')]"/>
                <filter string="Expired" name="filter_expired" domain="[('state', '=', 'expired')]"/>
                <filter string="Revoked" name="filter_revoked" domain="[('state', '=', 'revoked')]"/>
                <separator/>
                <filter string="For litigation" name="filter_judicial" domain="[('poa_type', '=', 'judicial')]"/>
                <filter string="I am an agent" name="filter_mine" domain="[('agent_user_ids', 'in', [uid])]"/>
                <group>
                    <filter string="Principal" name="group_principal" context="{'group_by': 'principal_company_id'}"/>
                    <filter string="Type" name="group_type" context="{'group_by': 'poa_type'}"/>
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_poa" model="ir.actions.act_window">
        <field name="name">Powers of attorney</field>
        <field name="res_model">legal.poa</field>
        <field name="path">powers-of-attorney</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_ldm_poa_search"/>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_ldm_poa_list')}),
            (0, 0, {'view_mode': 'kanban', 'view_id': ref('view_ldm_poa_kanban')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_ldm_poa_form')})]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No powers of attorney yet</p>
            <p>Register each power of attorney with its number, notary office and expiry date. You will be reminded before it expires, and the matters relying on it are told when it is revoked.</p>
        </field>
    </record>

    <record id="view_ldm_poa_revoke_wizard_form" model="ir.ui.view">
        <field name="name">legal.poa.revoke.wizard.form</field>
        <field name="model">legal.poa.revoke.wizard</field>
        <field name="arch" type="xml">
            <form string="Revoke power of attorney">
                <group>
                    <field name="poa_id" readonly="1" options="{'no_open': True}"/>
                    <field name="date"/>
                    <field name="reason" placeholder="For example: the client ended the mandate by notarial notice 1234 of 20 September 2026."/>
                </group>
                <div invisible="not open_task_ids">
                    <p class="text-muted mb-1">These open matters rely on it; their responsible will be told:</p>
                    <field name="open_task_ids" readonly="1">
                        <list>
                            <field name="task_number"/>
                            <field name="name"/>
                            <field name="lawyer_id" widget="many2one_avatar_user"/>
                        </list>
                    </field>
                </div>
                <footer>
                    <button name="action_confirm" string="Revoke" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- The client dossier: who can act for this client. -->
    <record id="view_legal_company_form_reg" model="ir.ui.view">
        <field name="name">legal.company.form.registers</field>
        <field name="model">legal.company</field>
        <field name="inherit_id" ref="view_legal_company_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_ldm_view_poas" type="object" class="oe_stat_button" icon="fa-id-card-o"
                        invisible="not poa_count">
                    <field name="poa_count" widget="statinfo" string="Powers of attorney"/>
                </button>
            </xpath>
        </field>
    </record>

    <!-- ============================================================
         Correspondence (الصادر والوارد)
         ============================================================ -->
    <record id="view_ldm_correspondence_list" model="ir.ui.view">
        <field name="name">legal.correspondence.list</field>
        <field name="model">legal.correspondence</field>
        <field name="arch" type="xml">
            <list string="Correspondence" sample="1" decoration-muted="state == 'void'">
                <header>
                    <button name="action_ldm_register_book" type="object" string="Print the register book"
                            display="always"/>
                </header>
                <field name="number" string="Number"/>
                <field name="date"/>
                <field name="name"/>
                <field name="party_display"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-success="state == 'registered'"
                       decoration-muted="state == 'void'"/>
                <field name="reply_state" widget="badge" optional="show" invisible="reply_state == 'none'"
                       decoration-warning="reply_state == 'waiting'"
                       decoration-danger="reply_state == 'overdue'"
                       decoration-success="reply_state == 'answered'"/>
                <field name="direction" optional="hide"/>
                <field name="task_id" optional="hide"/>
                <field name="reply_due_date" optional="hide"/>
                <field name="sender_ref" optional="hide"/>
                <field name="assigned_user_id" widget="many2one_avatar_user" optional="hide"/>
                <field name="legal_company_id" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_correspondence_form" model="ir.ui.view">
        <field name="name">legal.correspondence.form</field>
        <field name="model">legal.correspondence</field>
        <field name="arch" type="xml">
            <form string="Letter">
                <field name="ldm_numbering" invisible="1"/>
                <field name="reply_done" invisible="1"/>
                <header>
                    <button name="action_register" string="Register" type="object" class="btn-primary"
                            invisible="state != 'draft' or not ldm_numbering or not id"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <button name="action_ldm_print_letter" string="Print" type="object"
                            invisible="direction != 'outgoing' or state == 'void' or not id"
                            groups="legal_department_management.group_ldm_clerk,legal_department_management.group_ldm_auditor"/>
                    <button name="action_ldm_open_matter" string="Open a matter" type="object" class="btn-primary"
                            invisible="direction != 'incoming' or task_id or state != 'registered'"
                            groups="legal_department_management.group_ldm_clerk"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,registered"/>
                </header>
                <sheet>
                    <widget name="web_ribbon" title="Void" bg_color="text-bg-danger" invisible="state != 'void'"/>
                    <div class="alert alert-danger mb-3" role="alert" invisible="state != 'void'">
                        Void: <field name="void_reason" class="d-inline" readonly="1"/>
                    </div>
                    <div class="oe_title">
                        <div class="d-flex flex-wrap gap-3 text-muted">
                            <field name="direction" widget="radio" options="{'horizontal': true}"
                                   readonly="state != 'draft'"/>
                            <span invisible="not number and state != 'draft'">
                                <label for="number" class="me-1"/>
                                <field name="number" class="d-inline-block" readonly="state != 'draft'"
                                       placeholder="Given on registration"/>
                            </span>
                        </div>
                        <h1><field name="name" placeholder="Subject of the letter"/></h1>
                    </div>
                    <group name="essentials">
                        <group name="essentials_party">
                            <field name="date" readonly="state != 'draft'"/>
                            <field name="department_id" options="{'no_quick_create': True}"/>
                            <field name="partner_id" invisible="department_id and not partner_id"/>
                            <field name="task_id" options="{'no_create': True}"/>
                            <field name="legal_company_id" options="{'no_create': True}"/>
                        </group>
                        <group name="essentials_clock">
                            <field name="sender_ref" invisible="direction != 'incoming'"/>
                            <field name="sender_date" invisible="direction != 'incoming'"/>
                            <field name="received_date" invisible="direction != 'incoming'"/>
                            <field name="reply_to_id" invisible="not reply_to_id and state != 'draft'"
                                   options="{'no_create': True}"/>
                            <field name="reply_days" invisible="state != 'draft'"/>
                            <field name="reply_due_date" string="Answer due"/>
                            <field name="reply_state" widget="badge" invisible="reply_state == 'none'"
                                   decoration-warning="reply_state == 'waiting'"
                                   decoration-danger="reply_state == 'overdue'"
                                   decoration-success="reply_state == 'answered'"/>
                        </group>
                    </group>
                    <notebook>
                        <page name="letter" string="Letter" invisible="direction != 'outgoing'">
                            <group>
                                <group>
                                    <field name="template_id" options="{'no_create': True}"
                                           readonly="state != 'draft'"/>
                                    <field name="lang"/>
                                </group>
                                <group>
                                    <field name="signatory_id" options="{'no_create': True}"/>
                                    <field name="signatory_title"/>
                                </group>
                            </group>
                            <field name="body_html" placeholder="Write the letter, or choose a template." readonly="state == 'void'"/>
                            <group>
                                <field name="cc_lines" placeholder="One addressee per line"/>
                            </group>
                        </page>
                        <page name="referral" string="Referral" invisible="direction != 'incoming'">
                            <group>
                                <group>
                                    <field name="referred_by"/>
                                    <field name="assigned_user_id" widget="many2one_avatar_user"/>
                                </group>
                                <group>
                                    <field name="instruction_days" invisible="state != 'draft'"/>
                                    <field name="instruction_due"/>
                                </group>
                            </group>
                            <field name="referral_note" placeholder="The instruction written on the letter (هامش)."/>
                        </page>
                        <page name="files" string="Files">
                            <field name="attachment_ids" widget="many2many_binary"/>
                        </page>
                        <page name="replies" string="Replies" invisible="not reply_ids">
                            <field name="reply_ids" readonly="1">
                                <list>
                                    <field name="number"/>
                                    <field name="date"/>
                                    <field name="direction"/>
                                    <field name="name"/>
                                    <field name="state" widget="badge"/>
                                </list>
                            </field>
                        </page>
                        <page name="links" string="Links" invisible="not guarantee_id and not poa_id">
                            <group>
                                <field name="guarantee_id" readonly="1"/>
                                <field name="poa_id" readonly="1"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_correspondence_search" model="ir.ui.view">
        <field name="name">legal.correspondence.search</field>
        <field name="model">legal.correspondence</field>
        <field name="arch" type="xml">
            <search string="Correspondence">
                <field name="name" string="Letter"
                       filter_domain="['|', '|', ('name', 'ilike', self), ('number', 'ilike', self), ('sender_ref', 'ilike', self)]"/>
                <field name="department_id"/>
                <field name="partner_id"/>
                <field name="task_id"/>
                <field name="legal_company_id"/>
                <field name="assigned_user_id"/>
                <filter string="Incoming" name="filter_incoming" domain="[('direction', '=', 'incoming')]"/>
                <filter string="Outgoing" name="filter_outgoing" domain="[('direction', '=', 'outgoing')]"/>
                <separator/>
                <filter string="Not registered" name="filter_draft" domain="[('state', '=', 'draft')]"/>
                <filter string="Registered" name="filter_registered" domain="[('state', '=', 'registered')]"/>
                <filter string="Void" name="filter_void" domain="[('state', '=', 'void')]"/>
                <separator/>
                <filter string="Awaiting an answer" name="filter_awaiting" domain="[('reply_state', 'in', ('waiting', 'overdue'))]"/>
                <filter string="Answer overdue" name="filter_overdue" domain="[('reply_state', '=', 'overdue')]"/>
                <filter string="Referred to me" name="filter_mine" domain="[('assigned_user_id', '=', uid)]"/>
                <separator/>
                <filter string="Date" name="filter_date" date="date"/>
                <group>
                    <filter string="Direction" name="group_direction" context="{'group_by': 'direction'}"/>
                    <filter string="Body" name="group_body" context="{'group_by': 'department_id'}"/>
                    <filter string="Month" name="group_month" context="{'group_by': 'date:month'}"/>
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                </group>
                <searchpanel>
                    <field name="direction" icon="fa-exchange" enable_counters="1"/>
                    <field name="state" icon="fa-flag" enable_counters="1"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <record id="action_ldm_correspondence" model="ir.actions.act_window">
        <field name="name">Correspondence</field>
        <field name="res_model">legal.correspondence</field>
        <field name="path">correspondence</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ldm_correspondence_search"/>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_ldm_correspondence_list')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_ldm_correspondence_form')})]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">The register is empty</p>
            <p>Record each letter that arrives or leaves. Registering it gives it the next number of its book for the year; a mistake is voided with a reason, never deleted.</p>
        </field>
    </record>

    <record id="view_ldm_correspondence_void_wizard_form" model="ir.ui.view">
        <field name="name">legal.correspondence.void.wizard.form</field>
        <field name="model">legal.correspondence.void.wizard</field>
        <field name="arch" type="xml">
            <form string="Void letter">
                <group>
                    <field name="correspondence_id" readonly="1" options="{'no_open': True}"/>
                    <field name="reason" placeholder="For example: sent to the wrong directorate; replaced by letter 2026/58."/>
                </group>
                <footer>
                    <button name="action_confirm" string="Void" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_register_book_wizard_form" model="ir.ui.view">
        <field name="name">legal.register.book.wizard.form</field>
        <field name="model">legal.register.book.wizard</field>
        <field name="arch" type="xml">
            <form string="Print the register book">
                <group>
                    <field name="direction" widget="radio"/>
                    <field name="date_from"/>
                    <field name="date_to"/>
                </group>
                <footer>
                    <button name="action_print" string="Print" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Less frequent letter actions live in the gear menu. -->
    <record id="action_server_ldm_letter_void" model="ir.actions.server">
        <field name="name">Void</field>
        <field name="model_id" ref="model_legal_correspondence"/>
        <field name="binding_model_id" ref="model_legal_correspondence"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_clerk'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_void()</field>
    </record>
    <record id="action_server_ldm_letter_reply" model="ir.actions.server">
        <field name="name">Reply</field>
        <field name="model_id" ref="model_legal_correspondence"/>
        <field name="binding_model_id" ref="model_legal_correspondence"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_clerk'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_reply()</field>
    </record>
    <record id="action_server_ldm_letter_answered" model="ir.actions.server">
        <field name="name">Mark as answered</field>
        <field name="model_id" ref="model_legal_correspondence"/>
        <field name="binding_model_id" ref="model_legal_correspondence"/>
        <field name="binding_view_types">form,list</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_clerk'))]"/>
        <field name="state">code</field>
        <field name="code">records.action_ldm_mark_answered()</field>
    </record>
    <record id="action_server_ldm_letter_matter" model="ir.actions.server">
        <field name="name">Open a matter from this letter</field>
        <field name="model_id" ref="model_legal_correspondence"/>
        <field name="binding_model_id" ref="model_legal_correspondence"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_clerk'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_open_matter()</field>
    </record>
    <record id="action_server_ldm_letter_file_pdf" model="ir.actions.server">
        <field name="name">File the PDF on the matter</field>
        <field name="model_id" ref="model_legal_correspondence"/>
        <field name="binding_model_id" ref="model_legal_correspondence"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_clerk'))]"/>
        <field name="state">code</field>
        <field name="code">record.action_ldm_attach_pdf()</field>
    </record>

    <!-- ============================================================
         Letter templates and "New document from a template"
         ============================================================ -->
    <record id="view_ldm_letter_template_list" model="ir.ui.view">
        <field name="name">legal.letter.template.list</field>
        <field name="model">legal.letter.template</field>
        <field name="arch" type="xml">
            <list string="Letter templates">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="direction"/>
                <field name="usage_note"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_letter_template_form" model="ir.ui.view">
        <field name="name">legal.letter.template.form</field>
        <field name="model">legal.letter.template</field>
        <field name="arch" type="xml">
            <form string="Letter template">
                <sheet>
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <field name="active" invisible="1"/>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="e.g. Request for a good-standing certificate"/></h1>
                    </div>
                    <group>
                        <field name="direction" widget="radio" options="{'horizontal': true}"/>
                        <field name="usage_note"/>
                        <field name="subject" placeholder="e.g. Update on {matter}"/>
                    </group>
                    <notebook>
                        <page name="body" string="Text">
                            <field name="body" placeholder="Plain text. Use the placeholders on the next tab, in braces."/>
                        </page>
                        <page name="placeholders" string="Placeholders">
                            <p class="text-muted">Write a placeholder in braces, for example {client}. Only these names are filled; anything else in braces is printed as written, so a typo shows on the draft.</p>
                            <table class="table table-sm o_ldm_placeholders">
                                <thead><tr><th>Placeholder</th><th>Filled with</th></tr></thead>
                                <tbody>
                                    <tr><td>{today}</td><td>Today's date</td></tr>
                                    <tr><td>{company}</td><td>Our company or office</td></tr>
                                    <tr><td>{matter_number}</td><td>The matter number</td></tr>
                                    <tr><td>{matter}</td><td>The matter title</td></tr>
                                    <tr><td>{client}</td><td>The client</td></tr>
                                    <tr><td>{client_address}</td><td>The client's address</td></tr>
                                    <tr><td>{client_registration}</td><td>The client's registration number</td></tr>
                                    <tr><td>{body}</td><td>The body or court the letter goes to</td></tr>
                                    <tr><td>{body_addressee}</td><td>How letters to that body are addressed</td></tr>
                                    <tr><td>{court_case_number}</td><td>The court case number</td></tr>
                                    <tr><td>{opponent}</td><td>The opposing party</td></tr>
                                    <tr><td>{our_role}</td><td>Our role in the lawsuit</td></tr>
                                    <tr><td>{responsible}</td><td>The lawyer responsible for the matter</td></tr>
                                    <tr><td>{letter_number}, {letter_date}</td><td>The letter's register number and date</td></tr>
                                    <tr><td>{their_number}, {their_date}</td><td>The number and date of the letter we answer</td></tr>
                                    <tr><td>{subject}</td><td>The letter's subject</td></tr>
                                    <tr><td>{signatory}, {signatory_title}</td><td>Who signs, and their title</td></tr>
                                    <tr><td>{poa_number}, {poa_date}, {poa_notary}</td><td>The power of attorney</td></tr>
                                    <tr><td>{guarantee_number}, {guarantee_amount}, {guarantee_expiry}, {bank}, {beneficiary}</td><td>The letter of guarantee</td></tr>
                                    <tr><td>{opinion_number}</td><td>The number of the issued opinion</td></tr>
                                </tbody>
                            </table>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_ldm_letter_template" model="ir.actions.act_window">
        <field name="name">Letter templates</field>
        <field name="res_model">legal.letter.template</field>
        <field name="view_mode">list,form</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_ldm_letter_template_list')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_ldm_letter_template_form')})]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No letter templates</p>
            <p>Write the letters you send often once, with placeholders such as {client}; every matter can then produce them in one step.</p>
        </field>
    </record>

    <record id="view_ldm_document_wizard_form" model="ir.ui.view">
        <field name="name">legal.document.wizard.form</field>
        <field name="model">legal.document.wizard</field>
        <field name="arch" type="xml">
            <form string="New document from a template">
                <field name="task_id" invisible="1"/>
                <field name="guarantee_id" invisible="1"/>
                <field name="poa_id" invisible="1"/>
                <group>
                    <group>
                        <field name="template_id" options="{'no_create': True, 'no_open': True}"/>
                        <field name="department_id" options="{'no_create': True}"/>
                        <field name="partner_id" invisible="department_id and not partner_id"/>
                    </group>
                    <group>
                        <field name="lang"/>
                        <field name="signatory_id" options="{'no_create': True}"/>
                        <field name="signatory_title"/>
                    </group>
                </group>
                <field name="subject" placeholder="Subject" class="fs-5 fw-bold" invisible="not template_id"/>
                <field name="body_html" invisible="not template_id" options="{'height': 260}"/>
                <p class="text-muted" invisible="template_id">Choose a template: its text is filled from the matter and can be edited here before the document is created.</p>
                <footer>
                    <button name="action_create" string="Create the document" type="object" class="btn-primary"
                            invisible="not template_id"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- ============================================================
         Opening a matter from a request or a letter
         ============================================================ -->
    <record id="view_ldm_reg_matter_wizard_form" model="ir.ui.view">
        <field name="name">legal.reg.matter.wizard.form</field>
        <field name="model">legal.reg.matter.wizard</field>
        <field name="arch" type="xml">
            <form string="Open a matter">
                <field name="request_id" invisible="1"/>
                <field name="correspondence_id" invisible="1"/>
                <div class="alert alert-light border mb-3" role="status" invisible="not source_summary">
                    <field name="source_summary" class="text-muted"/>
                </div>
                <group>
                    <field name="template_id" options="{'no_create': True, 'no_open': True}"/>
                    <field name="legal_company_id" options="{'no_quick_create': True}"/>
                    <field name="name"/>
                    <field name="key_date"/>
                    <field name="department_id" options="{'no_create': True}"/>
                    <field name="lawyer_id" widget="many2one_avatar_user" options="{'no_create': True}"/>
                </group>
                <footer>
                    <button name="action_create" string="Open the matter" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- ============================================================
         Requests from other departments
         ============================================================ -->
    <record id="view_ldm_request_list_mine" model="ir.ui.view">
        <field name="name">legal.request.list.mine</field>
        <field name="model">legal.request</field>
        <field name="priority">20</field>
        <field name="arch" type="xml">
            <list string="My requests" sample="1">
                <field name="name"/>
                <field name="request_type"/>
                <field name="needed_by"/>
                <field name="state" widget="badge"
                       decoration-info="state in ('new', 'in_review')"
                       decoration-warning="state == 'returned'"
                       decoration-success="state == 'accepted'"
                       decoration-muted="state == 'rejected'"/>
                <field name="matter_state" string="Matter"/>
                <field name="matter_next_date"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_request_kanban" model="ir.ui.view">
        <field name="name">legal.request.kanban</field>
        <field name="model">legal.request</field>
        <field name="arch" type="xml">
            <kanban class="o_ldm_kanban" sample="1" quick_create="false">
                <field name="name"/>
                <field name="request_type"/>
                <field name="needed_by"/>
                <field name="state"/>
                <field name="matter_state"/>
                <field name="matter_next_date"/>
                <field name="requester_id"/>
                <templates>
                    <t t-name="card">
                        <field name="name" class="fw-bold"/>
                        <div class="text-muted small text-truncate">
                            <field name="request_type"/>
                            <t t-if="record.needed_by.raw_value"> · <field name="needed_by"/></t>
                        </div>
                        <div class="small" t-if="record.matter_state.raw_value">
                            <field name="matter_state"/>
                            <t t-if="record.matter_next_date.raw_value"> · <field name="matter_next_date"/></t>
                        </div>
                        <footer class="pt-1 align-items-center">
                            <field name="state" widget="badge"
                                   decoration-info="state in ('new', 'in_review')"
                                   decoration-warning="state == 'returned'"
                                   decoration-success="state == 'accepted'"
                                   decoration-muted="state == 'rejected'"/>
                            <field name="requester_id" widget="many2one_avatar_user" class="ms-auto"/>
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_ldm_request_list" model="ir.ui.view">
        <field name="name">legal.request.list</field>
        <field name="model">legal.request</field>
        <field name="arch" type="xml">
            <list string="Requests" sample="1">
                <field name="name"/>
                <field name="requester_id" widget="many2one_avatar_user"/>
                <field name="request_type"/>
                <field name="needed_by"/>
                <field name="assigned_user_id" widget="many2one_avatar_user"/>
                <field name="state" widget="badge"
                       decoration-info="state in ('new', 'in_review')"
                       decoration-warning="state == 'returned'"
                       decoration-success="state == 'accepted'"
                       decoration-muted="state == 'rejected'"/>
                <field name="legal_company_id" optional="hide"/>
                <field name="task_id" optional="hide"/>
                <field name="date_submitted" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_request_form" model="ir.ui.view">
        <field name="name">legal.request.form</field>
        <field name="model">legal.request</field>
        <field name="arch" type="xml">
            <form string="Request to the legal team">
                <field name="ldm_is_legal" invisible="1"/>
                <field name="ldm_is_requester" invisible="1"/>
                <field name="ldm_can_triage" invisible="1"/>
                <header>
                    <button name="action_ldm_take" string="Take it" type="object"
                            invisible="not ldm_can_triage or state != 'new'"
                            groups="legal_department_management.group_legal_user"/>
                    <button name="action_ldm_accept" string="Accept" type="object" class="btn-primary"
                            invisible="not ldm_can_triage"
                            groups="legal_department_management.group_legal_user"/>
                    <button name="action_ldm_return" string="Return" type="object"
                            invisible="not ldm_can_triage or state != 'in_review'"
                            groups="legal_department_management.group_legal_user"/>
                    <button name="action_ldm_resubmit" string="Send again" type="object" class="btn-primary"
                            invisible="state != 'returned' or not ldm_is_requester"/>
                    <button name="action_ldm_send_result" string="Send the result" type="object" class="btn-primary"
                            invisible="state != 'accepted'"
                            groups="legal_department_management.group_legal_user"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,in_review,accepted"
                           invisible="not id"/>
                </header>
                <sheet>
                    <div class="alert alert-warning mb-3" role="alert" invisible="state != 'returned'">
                        <b>Returned to you:</b> <field name="return_reason" class="d-inline" readonly="1"/>
                        <div invisible="not ldm_is_requester">Complete the request, then press “Send again”.</div>
                    </div>
                    <div class="alert alert-secondary mb-3" role="status" invisible="state != 'rejected'">
                        <b>Declined:</b> <field name="return_reason" class="d-inline" readonly="1"/>
                    </div>
                    <div class="alert alert-success mb-3" role="status" invisible="state != 'accepted'">
                        The legal team opened matter <b><field name="matter_number" class="d-inline"/></b>.
                        Status: <field name="matter_state" class="d-inline"/>
                        <span invisible="not matter_next_date"> · next date <field name="matter_next_date" class="d-inline"/></span>
                    </div>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="e.g. Review the supply contract with Al-Nahrain Company"
                                   readonly="state not in ('new', 'returned') and not ldm_is_legal"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="request_type" readonly="id and not ldm_is_legal"/>
                            <field name="needed_by" readonly="state not in ('new', 'returned') and not ldm_is_legal"/>
                        </group>
                        <group groups="legal_department_management.group_ldm_clerk,legal_department_management.group_ldm_auditor">
                            <field name="requester_id" widget="many2one_avatar_user" readonly="1"/>
                            <field name="legal_company_id" options="{'no_quick_create': True}"/>
                            <field name="assigned_user_id" widget="many2one_avatar_user" readonly="1"/>
                            <field name="task_id" readonly="1" invisible="not task_id"/>
                        </group>
                    </group>
                    <field name="description" readonly="state not in ('new', 'returned') and not ldm_is_legal"
                           placeholder="What is the question or the situation? Who is the other party? What has been done so far?"/>
                    <field name="attachment_ids" widget="many2many_binary"
                           readonly="state not in ('new', 'returned') and not ldm_is_legal"/>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_request_search" model="ir.ui.view">
        <field name="name">legal.request.search</field>
        <field name="model">legal.request</field>
        <field name="arch" type="xml">
            <search string="Requests">
                <field name="name"/>
                <field name="requester_id"/>
                <field name="legal_company_id"/>
                <field name="assigned_user_id"/>
                <filter string="To handle" name="filter_to_handle" domain="[('state', 'in', ('new', 'in_review'))]"/>
                <filter string="Mine to handle" name="filter_mine" domain="[('assigned_user_id', '=', uid)]"/>
                <filter string="Returned" name="filter_returned" domain="[('state', '=', 'returned')]"/>
                <filter string="Accepted" name="filter_accepted" domain="[('state', '=', 'accepted')]"/>
                <filter string="Declined" name="filter_declined" domain="[('state', '=', 'rejected')]"/>
                <separator/>
                <filter string="Needed within 7 days" name="filter_soon"
                        domain="[('needed_by', '!=', False), ('needed_by', '&lt;=', (context_today() + relativedelta(days=7)).strftime('%Y-%m-%d'))]"/>
                <group>
                    <filter string="Kind of help" name="group_type" context="{'group_by': 'request_type'}"/>
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                    <filter string="Handled by" name="group_assigned" context="{'group_by': 'assigned_user_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_request" model="ir.actions.act_window">
        <field name="name">Requests</field>
        <field name="res_model">legal.request</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="search_view_id" ref="view_ldm_request_search"/>
        <field name="context">{'search_default_filter_to_handle': 1, 'create': False}</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_ldm_request_list')}),
            (0, 0, {'view_mode': 'kanban', 'view_id': ref('view_ldm_request_kanban')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_ldm_request_form')})]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No request is waiting</p>
            <p>Requests from other departments arrive here. Take one, then accept it (a matter is opened), return it for completion, or decline it.</p>
        </field>
    </record>

    <record id="action_ldm_request_mine" model="ir.actions.act_window">
        <field name="name">My requests</field>
        <field name="res_model">legal.request</field>
        <field name="path">legal-requests</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="domain">[('requester_id', '=', uid)]</field>
        <field name="search_view_id" ref="view_ldm_request_search"/>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_ldm_request_list_mine')}),
            (0, 0, {'view_mode': 'kanban', 'view_id': ref('view_ldm_request_kanban')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_ldm_request_form')})]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">Ask the legal team</p>
            <p>Press <b>New</b>, say what you need, by when, and attach the papers. You can follow the answer here.</p>
        </field>
    </record>

    <record id="view_ldm_request_reason_wizard_form" model="ir.ui.view">
        <field name="name">legal.request.reason.wizard.form</field>
        <field name="model">legal.request.reason.wizard</field>
        <field name="arch" type="xml">
            <form string="Answer the request">
                <field name="mode" invisible="1"/>
                <group>
                    <field name="request_id" readonly="1" options="{'no_open': True}"/>
                    <field name="reason" invisible="mode != 'return'"
                           placeholder="What is missing? For example: attach the signed draft and the other party's registration certificate."/>
                    <field name="reason" invisible="mode != 'decline'" string="Why"
                           placeholder="For example: this is a commercial decision, not a legal question."/>
                </group>
                <footer>
                    <button name="action_confirm" string="Return to the requester" type="object" class="btn-primary"
                            invisible="mode != 'return'"/>
                    <button name="action_confirm" string="Decline" type="object" class="btn-primary"
                            invisible="mode != 'decline'"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="view_ldm_request_result_wizard_form" model="ir.ui.view">
        <field name="name">legal.request.result.wizard.form</field>
        <field name="model">legal.request.result.wizard</field>
        <field name="arch" type="xml">
            <form string="Send the result">
                <field name="available_attachment_ids" invisible="1"/>
                <group>
                    <field name="request_id" readonly="1" options="{'no_open': True}"/>
                    <field name="attachment_ids" widget="many2many_checkboxes"/>
                    <field name="message"/>
                </group>
                <p class="text-muted" invisible="available_attachment_ids">The matter has no files yet: the requester will receive the message only.</p>
                <footer>
                    <button name="action_send" string="Send" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_server_ldm_request_decline" model="ir.actions.server">
        <field name="name">Decline the request</field>
        <field name="model_id" ref="model_legal_request"/>
        <field name="binding_model_id" ref="model_legal_request"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_legal_user'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_decline()</field>
    </record>
    <record id="action_server_ldm_request_return" model="ir.actions.server">
        <field name="name">Return to the requester</field>
        <field name="model_id" ref="model_legal_request"/>
        <field name="binding_model_id" ref="model_legal_request"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_legal_user'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_return()</field>
    </record>

    <!-- ============================================================
         The matter: opinions, letters, documents, the power of attorney
         ============================================================ -->
    <record id="view_legal_task_form_reg" model="ir.ui.view">
        <field name="name">legal.task.form.registers</field>
        <field name="model">legal.task</field>
        <field name="inherit_id" ref="view_legal_task_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='ldm_is_manager']" position="after">
                <field name="ldm_can_issue_opinion" invisible="1"/>
                <field name="ldm_poa_state" invisible="1"/>
                <field name="opinion_memo_id" invisible="1"/>
                <field name="letter_count" invisible="1"/>
                <field name="poa_id" invisible="1"/>
            </xpath>
            <xpath expr="//header/field[@name='state']" position="before">
                <button name="action_issue_opinion" string="Issue opinion" type="object" class="btn-primary"
                        invisible="not ldm_can_issue_opinion"
                        groups="legal_department_management.group_ldm_approver"/>
            </xpath>
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_ldm_view_letters" type="object" class="oe_stat_button" icon="fa-envelope-o"
                        invisible="not letter_count">
                    <field name="letter_count" widget="statinfo" string="Letters"/>
                </button>
            </xpath>
            <xpath expr="//div[hasclass('oe_title')]" position="before">
                <div class="alert alert-danger mb-3" role="alert"
                     invisible="not poa_id or ldm_poa_state == 'active'">
                    The power of attorney this matter relies on is no longer valid
                    (<field name="ldm_poa_state" class="d-inline" readonly="1"/>).
                    Record a valid one before the next step.
                </div>
            </xpath>
            <xpath expr="//page[@name='documents']/field[@name='attachment_ids']" position="before">
                <div class="mt-2 mb-2" groups="legal_department_management.group_ldm_clerk">
                    <button name="action_ldm_new_document" type="object" class="btn btn-link px-0"
                            icon="fa-file-text-o" string="New document from a template"/>
                </div>
            </xpath>
            <xpath expr="//field[@name='opinion_date']" position="after">
                <field name="opinion_issued_by_id" invisible="not opinion_issued_by_id"/>
            </xpath>
            <xpath expr="//field[@name='question']" position="attributes">
                <attribute name="readonly">opinion_number</attribute>
            </xpath>
            <xpath expr="//field[@name='requesting_unit']" position="attributes">
                <attribute name="readonly">opinion_number</attribute>
            </xpath>
            <xpath expr="//field[@name='opinion_html']" position="attributes">
                <attribute name="readonly">opinion_number</attribute>
            </xpath>
            <xpath expr="//field[@name='opinion_html']" position="after">
                <div class="d-flex flex-wrap gap-3 align-items-center mt-2" invisible="not opinion_number">
                    <button name="action_ldm_download_memo" type="object" class="btn btn-link px-0"
                            icon="fa-download" string="Download the opinion memo" invisible="not opinion_memo_id"/>
                    <span class="text-muted" invisible="superseded_by_id">Issued and frozen. To change it, revise it from the gear menu.</span>
                    <span class="text-warning" invisible="not superseded_by_id">Replaced by a revision.</span>
                </div>
            </xpath>
        </field>
    </record>

    <record id="view_legal_task_search_reg" model="ir.ui.view">
        <field name="name">legal.task.search.registers</field>
        <field name="model">legal.task</field>
        <field name="inherit_id" ref="view_legal_task_search"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='filter_confidential']" position="after">
                <filter string="Power of attorney not valid" name="filter_poa_not_valid"
                        domain="[('poa_id.state', 'in', ('revoked', 'expired'))]"/>
            </xpath>
        </field>
    </record>

    <record id="action_server_ldm_new_document" model="ir.actions.server">
        <field name="name">New document from a template</field>
        <field name="model_id" ref="model_legal_task"/>
        <field name="binding_model_id" ref="model_legal_task"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_ldm_clerk'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_new_document()</field>
    </record>
    <record id="action_server_ldm_revise_opinion" model="ir.actions.server">
        <field name="name">Revise opinion</field>
        <field name="model_id" ref="model_legal_task"/>
        <field name="binding_model_id" ref="model_legal_task"/>
        <field name="binding_view_types">form</field>
        <field name="group_ids" eval="[(4, ref('group_legal_user'))]"/>
        <field name="state">code</field>
        <field name="code">action = record.action_ldm_revise_opinion()</field>
    </record>

    <!-- The register of issued opinions, with full-text search. -->
    <record id="view_ldm_opinion_list" model="ir.ui.view">
        <field name="name">legal.task.opinion.list</field>
        <field name="model">legal.task</field>
        <field name="mode">primary</field>
        <field name="priority">40</field>
        <field name="arch" type="xml">
            <list string="Legal opinions" create="0" decoration-muted="superseded_by_id">
                <field name="opinion_number"/>
                <field name="opinion_date"/>
                <field name="name" string="Subject"/>
                <field name="requesting_unit"/>
                <field name="legal_company_id"/>
                <field name="superseded_by_id" string="Replaced by" optional="show"/>
                <field name="task_number" optional="hide"/>
                <field name="lawyer_id" widget="many2one_avatar_user" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_opinion_search" model="ir.ui.view">
        <field name="name">legal.task.opinion.search</field>
        <field name="model">legal.task</field>
        <field name="mode">primary</field>
        <field name="priority">40</field>
        <field name="arch" type="xml">
            <search string="Legal opinions">
                <field name="question" string="Words in the question or the opinion"
                       filter_domain="['|', '|', ('question', 'ilike', self), ('opinion_html', 'ilike', self), ('name', 'ilike', self)]"/>
                <field name="opinion_number"/>
                <field name="requesting_unit"/>
                <field name="legal_company_id"/>
                <filter string="Issued" name="filter_issued" domain="[('opinion_number', '!=', False)]"/>
                <filter string="In force" name="filter_current"
                        domain="[('opinion_number', '!=', False), ('superseded_by_id', '=', False)]"/>
                <filter string="Replaced" name="filter_superseded" domain="[('superseded_by_id', '!=', False)]"/>
                <filter string="Not issued yet" name="filter_draft" domain="[('opinion_number', '=', False)]"/>
                <separator/>
                <filter string="Issued on" name="filter_opinion_date" date="opinion_date"/>
                <group>
                    <filter string="Asked by" name="group_unit" context="{'group_by': 'requesting_unit'}"/>
                    <filter string="Year" name="group_year" context="{'group_by': 'opinion_date:year'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_opinion_register" model="ir.actions.act_window">
        <field name="name">Legal opinions</field>
        <field name="res_model">legal.task</field>
        <field name="path">legal-opinions</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('kind', '=', 'opinion')]</field>
        <field name="context">{'search_default_filter_issued': 1, 'create': False}</field>
        <field name="search_view_id" ref="view_ldm_opinion_search"/>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_ldm_opinion_list')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_legal_task_form')})]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No opinion issued yet</p>
            <p>Open a matter of type Legal opinion, write the question and the opinion, and issue it: it gets its number for the year and appears here, searchable by its words.</p>
        </field>
    </record>

    <menuitem id="menu_ldm_opinions" name="Legal opinions" parent="menu_ldm_registers" sequence="4"
              action="action_ldm_opinion_register"/>

    <!-- ============================================================
         Letters of guarantee
         ============================================================ -->
    <record id="view_ldm_guarantee_list" model="ir.ui.view">
        <field name="name">legal.guarantee.list</field>
        <field name="model">legal.guarantee</field>
        <field name="arch" type="xml">
            <list string="Letters of guarantee" sample="1"
                  decoration-muted="state in ('released', 'liquidated', 'expired')">
                <field name="name"/>
                <field name="legal_company_id"/>
                <field name="bank_id"/>
                <field name="amount" options="{'hide_trailing_zeros': True}"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="date_expiry" widget="remaining_days"/>
                <field name="state" widget="badge"
                       decoration-success="state in ('active', 'extended')"
                       decoration-warning="state == 'extension_requested'"
                       decoration-danger="state == 'expired'"
                       decoration-muted="state in ('released', 'liquidated')"/>
                <field name="kind" optional="hide"/>
                <field name="beneficiary_id" optional="hide"/>
                <field name="number" optional="hide"/>
                <field name="task_id" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_ldm_guarantee_form" model="ir.ui.view">
        <field name="name">legal.guarantee.form</field>
        <field name="model">legal.guarantee</field>
        <field name="arch" type="xml">
            <form string="Letter of guarantee">
                <field name="is_expiring" invisible="1"/>
                <header>
                    <button name="action_ldm_request_extension" string="Ask the bank to extend" type="object"
                            class="btn-primary" invisible="state not in ('active', 'extended') or not id"
                            groups="legal_department_management.group_legal_user"/>
                    <button name="action_ldm_extend" string="Record the extension" type="object" class="btn-primary"
                            invisible="state != 'extension_requested'"
                            groups="legal_department_management.group_legal_user"/>
                    <button name="action_ldm_release" string="Release" type="object"
                            invisible="state not in ('active', 'extension_requested', 'extended') or not id"
                            groups="legal_department_management.group_legal_user"/>
                    <field name="state" widget="statusbar" statusbar_visible="active,extension_requested,extended,released"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_ldm_view_letters" type="object" class="oe_stat_button" icon="fa-envelope-o"
                                invisible="not correspondence_ids">
                            <div class="o_stat_info"><span class="o_stat_text">Letters</span></div>
                        </button>
                    </div>
                    <field name="correspondence_ids" invisible="1"/>
                    <div class="alert alert-warning mb-3" role="status" invisible="not is_expiring">
                        Expires in <field name="days_to_expiry" class="d-inline"/> days. Ask the bank to extend it,
                        or release it if the obligation is fulfilled.
                    </div>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="e.g. Performance guarantee — Basra water contract"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="kind"/>
                            <field name="legal_company_id" options="{'no_quick_create': True}"/>
                            <field name="bank_id"/>
                            <field name="number"/>
                            <field name="beneficiary_id"/>
                        </group>
                        <group>
                            <field name="amount" options="{'hide_trailing_zeros': True}"/>
                            <field name="currency_id" groups="base.group_multi_currency"/>
                            <field name="percent"/>
                            <field name="date_issued"/>
                            <field name="date_expiry"/>
                            <field name="release_date" invisible="not release_date"/>
                        </group>
                    </group>
                    <group>
                        <group>
                            <field name="task_id" options="{'no_create': True}"/>
                        </group>
                        <group>
                            <field name="scan_name" invisible="1"/>
                            <field name="scan" filename="scan_name"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_ldm_guarantee_search" model="ir.ui.view">
        <field name="name">legal.guarantee.search</field>
        <field name="model">legal.guarantee</field>
        <field name="arch" type="xml">
            <search string="Letters of guarantee">
                <field name="name" filter_domain="['|', ('name', 'ilike', self), ('number', 'ilike', self)]"/>
                <field name="legal_company_id"/>
                <field name="bank_id"/>
                <field name="beneficiary_id"/>
                <filter string="Live" name="filter_live" domain="[('state', 'in', ('active', 'extension_requested', 'extended'))]"/>
                <filter string="Expires within 30 days" name="filter_expiring" domain="[('is_expiring', '=', True)]"/>
                <filter string="Released" name="filter_released" domain="[('state', '=', 'released')]"/>
                <group>
                    <filter string="Kind" name="group_kind" context="{'group_by': 'kind'}"/>
                    <filter string="Bank" name="group_bank" context="{'group_by': 'bank_id'}"/>
                    <filter string="Status" name="group_state" context="{'group_by': 'state'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_ldm_guarantee" model="ir.actions.act_window">
        <field name="name">Letters of guarantee</field>
        <field name="res_model">legal.guarantee</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_ldm_guarantee_search"/>
        <field name="context">{'search_default_filter_live': 1}</field>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'list', 'view_id': ref('view_ldm_guarantee_list')}),
            (0, 0, {'view_mode': 'form', 'view_id': ref('view_ldm_guarantee_form')})]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No letters of guarantee</p>
            <p>Record the bid, performance and advance-payment guarantees: you are reminded 30 days before each one expires, and the letter to the bank is drafted for you.</p>
        </field>
    </record>

    <record id="view_ldm_guarantee_wizard_form" model="ir.ui.view">
        <field name="name">legal.guarantee.wizard.form</field>
        <field name="model">legal.guarantee.wizard</field>
        <field name="arch" type="xml">
            <form string="Letter of guarantee">
                <field name="mode" invisible="1"/>
                <group>
                    <field name="guarantee_id" readonly="1" options="{'no_open': True}"/>
                    <field name="current_expiry"/>
                    <field name="new_expiry" invisible="mode != 'extend'" required="mode == 'extend'"/>
                    <field name="date" invisible="mode != 'release'" string="Released on"/>
                    <field name="draft_letter" invisible="mode == 'extend'"/>
                    <field name="template_id" invisible="mode == 'extend' or not draft_letter"
                           options="{'no_create': True, 'no_open': True}"/>
                    <field name="lang" invisible="mode == 'extend' or not draft_letter"/>
                    <field name="note" placeholder="Optional note for the history"/>
                </group>
                <footer>
                    <button name="action_confirm" string="Confirm" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- ============================================================
         Reporting: the monthly status report and the exposure pivot
         ============================================================ -->
    <record id="view_ldm_monthly_report_wizard_form" model="ir.ui.view">
        <field name="name">legal.monthly.report.wizard.form</field>
        <field name="model">legal.monthly.report.wizard</field>
        <field name="arch" type="xml">
            <form string="Monthly status report">
                <p class="text-muted">Lawsuits for and against, matters opened and closed, sessions, judgments, pending challenges, missed periods, government transactions by body, and what expires in the next 60 days.</p>
                <group>
                    <field name="date_from"/>
                    <field name="date_to"/>
                </group>
                <footer>
                    <button name="action_print" string="Print" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_ldm_monthly_report_wizard" model="ir.actions.act_window">
        <field name="name">Monthly status report</field>
        <field name="res_model">legal.monthly.report.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <record id="view_ldm_exposure_pivot" model="ir.ui.view">
        <field name="name">legal.task.exposure.pivot</field>
        <field name="model">legal.task</field>
        <field name="priority">40</field>
        <field name="arch" type="xml">
            <pivot string="Litigation exposure" sample="1">
                <field name="our_role" type="row"/>
                <field name="court_stage" type="col"/>
                <field name="matter_value" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="action_ldm_exposure" model="ir.actions.act_window">
        <field name="name">Litigation exposure</field>
        <field name="res_model">legal.task</field>
        <field name="view_mode">pivot,list,form</field>
        <field name="domain">[('kind', 'in', ('litigation', 'execution')), ('state', 'in', ('draft', 'in_progress', 'pending_docs'))]</field>
        <field name="search_view_id" ref="view_legal_task_search"/>
        <field name="view_ids" eval="[(5, 0, 0),
            (0, 0, {'view_mode': 'pivot', 'view_id': ref('view_ldm_exposure_pivot')})]"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No open lawsuits</p>
            <p>Open lawsuits appear here by our role and court stage, with the value of the claims.</p>
        </field>
    </record>

    <menuitem id="menu_ldm_monthly_report" name="Monthly status report" parent="menu_ldm_reporting" sequence="6"
              action="action_ldm_monthly_report_wizard"/>
    <menuitem id="menu_ldm_exposure" name="Litigation exposure" parent="menu_ldm_reporting" sequence="7"
              action="action_ldm_exposure"/>
</odoo>
